---
phase: 260414-dg2-rebrand-emails-5-internal-notification-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/email/brand-layout.ts
  - lib/services/email.service.ts
  - prisma/schema.prisma
  - app/api/cron/seller-digest/route.ts
  - app/api/cron/finance-digest/route.ts
  - app/api/cron/admin-digest/route.ts
  - app/api/cron/overdue-invoices/route.ts
  - app/api/webhooks/docusign/route.ts
  - app/api/webhooks/quickbooks/route.ts
  - vercel.json
autonomous: true
requirements:
  - DG2-A: Shared Carreira USA brand layout helper
  - DG2-B: Rebrand all existing email templates
  - DG2-C1: Real-time seller invoice events (overdue + paid)
  - DG2-C2: Real-time seller contract events (signed + unsigned)
  - DG2-C3: Seller daily digest cron (8 AM)
  - DG2-C4: Finance daily digest cron (8 AM)
  - DG2-C5: Admin weekly digest cron (Mon 8 AM)
  - DG2-D: NotificationType enum extension + cron registration

must_haves:
  truths:
    - "Every email rendered by EmailService uses the shared Carreira USA brand layout (Verde header, Tangerina accents, Creme background, Cafe com Leite borders)"
    - "When an invoice transitions to OVERDUE, the deal/invoice owner (SALES user) receives a real-time PT-BR notification"
    - "When a customer pays an invoice, the invoice owner (SALES user) receives a real-time PT-BR notification"
    - "When DocuSign reports a contract as signed, the deal owner (SALES) receives a notification (in addition to or replacing finance-only routing)"
    - "When a contract expires or is declined/voided, the deal owner (SALES) receives a notification"
    - "At 08:00 daily, each active SALES user with non-empty digest data receives a PT-BR seller daily digest"
    - "At 08:00 daily, each active FINANCE user receives a PT-BR finance daily digest with AR aging, today's collections, sync errors, cashflow, and stale invoices"
    - "At 08:00 every Monday, each active ADMIN user receives a PT-BR admin weekly digest with MRR delta, deals at risk, latest CFO insights, conversion funnel, lead source perf, and BI highlights"
    - "Empty seller-digest sections are skipped (no email sent if all sections empty)"
    - "Seven new NotificationType enum values exist and are used by the corresponding sends for tracking"
  artifacts:
    - path: "lib/email/brand-layout.ts"
      provides: "renderBaseLayout(opts) helper exporting Carreira USA palette + Arial-fallback HTML shell"
      exports: ["renderBaseLayout", "BRAND_COLORS"]
    - path: "lib/services/email.service.ts"
      provides: "All existing templates rebranded via renderBaseLayout + 7 new internal methods (sendSellerInvoiceOverdue, sendSellerInvoicePaid, sendSellerContractSigned, sendSellerContractUnsigned, sendSellerDailyDigest, sendFinanceDailyDigest, sendAdminWeeklyDigest)"
      contains: "renderBaseLayout"
    - path: "prisma/schema.prisma"
      provides: "NotificationType enum extended with 7 new values"
      contains: "INVOICE_OVERDUE_SELLER"
    - path: "app/api/cron/seller-digest/route.ts"
      provides: "Daily seller digest cron handler"
      exports: ["POST"]
    - path: "app/api/cron/finance-digest/route.ts"
      provides: "Daily finance digest cron handler"
      exports: ["POST"]
    - path: "app/api/cron/admin-digest/route.ts"
      provides: "Weekly admin digest cron handler"
      exports: ["POST"]
    - path: "vercel.json"
      provides: "Cron registrations for the 3 new digests"
      contains: "/api/cron/seller-digest"
  key_links:
    - from: "lib/services/email.service.ts (every send* method)"
      to: "lib/email/brand-layout.ts"
      via: "import { renderBaseLayout, BRAND_COLORS }"
      pattern: "renderBaseLayout\\("
    - from: "app/api/cron/overdue-invoices/route.ts"
      to: "emailService.sendSellerInvoiceOverdue"
      via: "after marking invoice OVERDUE, look up Invoice.ownerId -> User and send"
      pattern: "sendSellerInvoiceOverdue"
    - from: "app/api/webhooks/quickbooks/route.ts"
      to: "emailService.sendSellerInvoicePaid"
      via: "on Payment event, resolve invoice -> ownerId -> User and send"
      pattern: "sendSellerInvoicePaid"
    - from: "app/api/webhooks/docusign/route.ts"
      to: "emailService.sendSellerContractSigned / sendSellerContractUnsigned"
      via: "on completed/declined/voided envelope status -> contract -> deal -> Deal.ownerId -> User"
      pattern: "sendSellerContract"
    - from: "vercel.json"
      to: "app/api/cron/{seller,finance,admin}-digest/route.ts"
      via: "crons[] entries with schedules 0 8 * * *, 0 8 * * *, 0 8 * * 1"
      pattern: "/api/cron/(seller|finance|admin)-digest"
---

<objective>
Rebrand every existing email template to the Carreira USA brand identity (Verde #2F443F, Tangerina #FF8142, Creme #FFF8E8, Cafe com Leite #E1C19B, Arial fallback) AND add five new internal notification streams: real-time seller invoice events, real-time seller contract events, seller daily digest, finance daily digest, and admin weekly digest.

Purpose: Stop relying on generic blue/red email templates that leak un-branded chrome to internal staff, and close the operational visibility gap so SALES, FINANCE, and ADMIN users receive proactive, role-scoped intelligence without having to log into the dashboard.

Output: A `lib/email/brand-layout.ts` shared helper, a fully rebranded `lib/services/email.service.ts` with 7 new internal templates, 3 new cron routes, hook-point wiring in 2 webhooks + 1 cron, an extended `NotificationType` enum (via `npm run db:push` since this project uses push for fast schema iteration per CLAUDE.md), and 3 new `vercel.json` cron entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@lib/services/email.service.ts
@prisma/schema.prisma
@vercel.json
@app/api/cron/daily-ar-digest/route.ts
@app/api/cron/overdue-invoices/route.ts
@app/api/webhooks/docusign/route.ts
@app/api/webhooks/quickbooks/route.ts
@lib/services/financial-bi.ts
@lib/services/invoice-workflow.service.ts

<interfaces>
<!-- Key contracts the executor needs. Use directly — no exploration required. -->

Brand palette (from CLAUDE.md + STATE.md decisions, must match):
  Verde      = #2F443F  (primary, headers, dark surfaces)
  Tangerina  = #FF8142  (accent — only on dark surfaces, fails WCAG on white)
  Creme      = #FFF8E8  (page background)
  Cafe Leite = #E1C19B  (borders, secondary surface)
  Text dark  = #2F443F
  Text muted = #6B6B6B
Email font stack: Arial, Helvetica, sans-serif (Blaak/Neue Montreal NOT email-safe).

renderBaseLayout signature (to create in lib/email/brand-layout.ts):
```typescript
export const BRAND_COLORS = {
  verde: '#2F443F',
  tangerina: '#FF8142',
  creme: '#FFF8E8',
  cafeLeite: '#E1C19B',
  textDark: '#2F443F',
  textMuted: '#6B6B6B',
  white: '#FFFFFF',
} as const;

export interface BaseLayoutOpts {
  title: string;            // appears in <head><title> and visually as H1 in header
  preheader?: string;       // hidden preview text
  bodyHtml: string;         // inner body content (already-rendered HTML)
  ctaLabel?: string;        // optional primary CTA button
  ctaUrl?: string;
  footerNote?: string;      // small text under footer divider
}

export function renderBaseLayout(opts: BaseLayoutOpts): string;
```

Existing EmailService methods to rebrand (do NOT change signatures, only HTML body):
  sendOverdueInvoiceAlert, sendDailyDigest, sendStaleInvoiceAlert,
  sendWelcomeWithTempPassword (already brand-correct — confirm and align),
  sendContractForSignature, sendContractSigned, sendContractExpired,
  sendPaymentLink, sendPaymentReminder, sendPaymentReceived,
  sendHubWelcome, sendHubInvoiceAvailable, sendHubPasswordReset,
  sendInvoiceApprovalRequest, sendInvoiceApproved, sendInvoiceRejected.

Schema relations (verified):
  Invoice.ownerId -> User (relation name "InvoiceOwner") — schema.prisma:311,326
  Deal.ownerId   -> User — schema.prisma:266,280
  Contract has dealId/invoiceId — resolve seller via Deal.ownerId; fall back to Invoice.ownerId.

NotificationType enum (schema.prisma:770) — APPEND these 7 values:
  INVOICE_OVERDUE_SELLER
  INVOICE_PAID_SELLER
  CONTRACT_SIGNED_SELLER
  CONTRACT_UNSIGNED_SELLER
  SELLER_DAILY_DIGEST
  FINANCE_DAILY_DIGEST
  ADMIN_WEEKLY_DIGEST

vercel.json cron entries to APPEND to crons[]:
  { "path": "/api/cron/seller-digest",  "schedule": "0 8 * * *" }
  { "path": "/api/cron/finance-digest", "schedule": "0 8 * * *" }
  { "path": "/api/cron/admin-digest",   "schedule": "0 8 * * 1" }

Cron auth pattern (from app/api/cron/daily-ar-digest/route.ts:30-42): verify Bearer ${CRON_SECRET}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create brand layout helper + extend Prisma enum</name>
  <files>lib/email/brand-layout.ts, prisma/schema.prisma</files>
  <action>
    1) Create `lib/email/brand-layout.ts`:
       - Export `BRAND_COLORS` constant exactly as in <interfaces>.
       - Export `renderBaseLayout(opts: BaseLayoutOpts): string` returning a complete HTML email document:
         * `<!DOCTYPE html>` + table-based layout (email-client-safe, 600px max-width, centered).
         * `<head>` includes `<title>{title}</title>` and meta viewport.
         * Hidden preheader span (`display:none; visibility:hidden; opacity:0; height:0; ...`) when `opts.preheader`.
         * Header band: background `BRAND_COLORS.verde`, white text, padding 24px, brand wordmark "Carreira USA" (Arial bold) + page title.
         * Body container: background `BRAND_COLORS.creme`, padding 32px 24px, border 1px solid `BRAND_COLORS.cafeLeite`, border-radius 8px. Inner HTML is `opts.bodyHtml` rendered as-is (executor inserts safe HTML — never accept user input here).
         * Optional CTA button: background `BRAND_COLORS.tangerina`, color `BRAND_COLORS.verde` (Tangerina on Verde passes contrast — WCAG-safe per STATE.md decision), 14px 28px padding, border-radius 6px, bold.
         * Footer: muted text `BRAND_COLORS.textMuted` 12px, "Carreira USA · Sistema interno" + optional `footerNote`.
         * Font stack everywhere: `Arial, Helvetica, sans-serif`.
       - All inline styles (no <style> blocks — Gmail strips them).

    2) Edit `prisma/schema.prisma` enum `NotificationType` (line 770) — append the 7 new values listed in <interfaces> after `HUB_PASSWORD_RESET`.

    3) Run `npm run db:generate && npm run db:push` (project convention per CLAUDE.md "Use db:push in development for quick schema iteration").
  </action>
  <verify>
    <automated>npx tsc --noEmit lib/email/brand-layout.ts && npx prisma validate && node -e "const {NotificationType}=require('@prisma/client'); ['INVOICE_OVERDUE_SELLER','INVOICE_PAID_SELLER','CONTRACT_SIGNED_SELLER','CONTRACT_UNSIGNED_SELLER','SELLER_DAILY_DIGEST','FINANCE_DAILY_DIGEST','ADMIN_WEEKLY_DIGEST'].forEach(v=>{if(!NotificationType[v])throw new Error('missing '+v)}); console.log('ok')"</automated>
  </verify>
  <done>
    `lib/email/brand-layout.ts` exports `renderBaseLayout` and `BRAND_COLORS`; Prisma client types include all 7 new NotificationType values; `npm run db:push` succeeded against the dev DB.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rebrand every existing template in email.service.ts via renderBaseLayout</name>
  <files>lib/services/email.service.ts</files>
  <action>
    For each of the following methods, replace the inline HTML string with a call to `renderBaseLayout(...)` from `lib/email/brand-layout.ts`. Preserve method signatures, return types, locale logic, and the `sendEmailWithTracking` / `sendEmailSimple` choice for each method.

    Methods to migrate (audit each — replace any hardcoded `#2563eb`, `#10b981`, `#dc2626`, `#f59e0b`, `#1f2937`, etc. with `BRAND_COLORS.*`):
      - sendOverdueInvoiceAlert
      - sendDailyDigest
      - sendStaleInvoiceAlert
      - sendWelcomeWithTempPassword (already brand-aligned per scope notes — confirm it now uses the shared helper instead of duplicated inline HTML)
      - sendContractForSignature
      - sendContractSigned
      - sendContractExpired
      - sendPaymentLink
      - sendPaymentReminder
      - sendPaymentReceived
      - sendHubWelcome
      - sendHubInvoiceAvailable
      - sendHubPasswordReset
      - sendInvoiceApprovalRequest
      - sendInvoiceApproved
      - sendInvoiceRejected

    Conventions:
      - `bodyHtml` should contain only the inner content (greeting, table, paragraphs) — do NOT re-include `<html>`, `<body>`, header, footer.
      - Use Tangerina ONLY for the CTA button, never for body text on Creme (WCAG fail per STATE.md v1.1 decision).
      - Status badges: keep semantic colors but use brand-tinted variants — Verde for success, Tangerina for warning/CTA, a darker red `#A8332B` for error (only on white pill background, never on Creme).
      - Customer-facing templates (sendHub*, sendPayment*, sendContractForSignature) keep their existing locale logic (PT-BR + EN); pass localized strings into `bodyHtml`.
      - Internal templates (sendOverdue*, sendDaily*, sendStale*, sendInvoiceApproval*, sendInvoiceApproved, sendInvoiceRejected, sendContractSigned, sendContractExpired) are PT-BR only.

    Add at the top of file:
      `import { renderBaseLayout, BRAND_COLORS } from '@/lib/email/brand-layout';`

    Do NOT touch `sendEmailWithTracking` or `sendEmailSimple` core methods.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -nE "#2563eb|#10b981|#dc2626|#f59e0b|background:\\s*#1f2937" lib/services/email.service.ts && exit 1 || echo "no legacy colors remain"</automated>
  </verify>
  <done>
    Every existing template in email.service.ts renders via `renderBaseLayout`; no hardcoded legacy hex colors remain in the file; `tsc --noEmit` passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add 7 new internal email methods (seller events + 3 digests)</name>
  <files>lib/services/email.service.ts</files>
  <action>
    Append these methods to `EmailService`. All PT-BR. All use `renderBaseLayout`. All use `sendEmailWithTracking` with the matching new `NotificationType` value (per scope recommendation — enum values are being added anyway).

    1) `sendSellerInvoiceOverdue(invoice: Invoice, seller: User): Promise<void>`
       - Type: `INVOICE_OVERDUE_SELLER`. Subject: `[Cobrança] Fatura {invoiceNumber} de {customerName} venceu`.
       - Body: greeting "Ola {seller.name}", days overdue, amount, customer, CTA "Ver fatura" -> `${APP_URL}/dashboard/invoices/{invoice.id}`.

    2) `sendSellerInvoicePaid(invoice: Invoice, seller: User): Promise<void>`
       - Type: `INVOICE_PAID_SELLER`. Subject: `Pagamento recebido — {customerName} ({amount})`.
       - Body: confirmation copy, amount, customer, CTA "Ver fatura".

    3) `sendSellerContractSigned(contract: Contract, seller: User, dealTitle?: string): Promise<void>`
       - Type: `CONTRACT_SIGNED_SELLER`. Subject: `Contrato assinado — {signerName}`.
       - Body: signed date, signer, deal title, CTA "Abrir contrato" -> `signedUrl` if present else dashboard contract link.

    4) `sendSellerContractUnsigned(contract: Contract, seller: User, reason: 'expired'|'declined'|'voided'): Promise<void>`
       - Type: `CONTRACT_UNSIGNED_SELLER`. Subject: `Contrato {reason==='expired'?'expirou':reason==='declined'?'foi recusado':'foi cancelado'} — {signerName}`.
       - Body: explain reason in PT-BR, suggested next action ("Reenvie ou ligue para o cliente"), CTA "Ver contrato".

    5) `sendSellerDailyDigest(seller: User, data: SellerDigestData): Promise<void>`
       - Type: `SELLER_DAILY_DIGEST`. Subject: `Seu resumo do dia — {date}`.
       - Sections (rendered as small HTML tables, hidden if empty):
           overdueInvoices: Array<{invoiceNumber, customerName, amount, daysOverdue}>
           unsignedContracts: Array<{signerName, dealTitle, daysSinceSent}>  // 7+ days
           staleDeals: Array<{title, value, lastActivityDays}>               // 14+ days
           unansweredLeads: Array<{name, source, hoursSinceCreated}>          // 24h+
       - Add `export interface SellerDigestData` near top of file with these shapes.
       - Caller is responsible for skipping empty digests (cron logic), but defensively no-op if all four arrays are empty.

    6) `sendFinanceDailyDigest(user: User, data: FinanceDigestData): Promise<void>`
       - Type: `FINANCE_DAILY_DIGEST`. Subject: `Resumo financeiro — {date}`.
       - Sections: arAging {bucket0to30, bucket31to60, bucket61to90, bucket90Plus}, todayExpectedCollections (number+amount), syncErrorInvoices (array of {id, invoiceNumber, errorMessage}), cashflow {revenueThisMonth, revenueLastMonth, deltaPercent}, staleInvoices (count+amount).
       - Export `FinanceDigestData` interface.

    7) `sendAdminWeeklyDigest(user: User, data: AdminDigestData): Promise<void>`
       - Type: `ADMIN_WEEKLY_DIGEST`. Subject: `Resumo executivo semanal — {weekRange}`.
       - Sections: mrr {current, deltaWeek, deltaPercent}, dealsAtRisk (top 5 array), cfoInsights (string — pre-rendered markdown from latest cfo-analysis, render as <pre> or simple paragraph), conversionFunnel {leads, qualified, deals, won}, leadSourcePerformance (array of {source, leads, conversions}), biHighlights (string array).
       - Export `AdminDigestData` interface.

    All seven methods follow the same shape as existing senders (try/catch handled inside `sendEmailWithTracking`). Recipients always come from the function args; do not hardcode addresses.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -cE "sendSellerInvoiceOverdue|sendSellerInvoicePaid|sendSellerContractSigned|sendSellerContractUnsigned|sendSellerDailyDigest|sendFinanceDailyDigest|sendAdminWeeklyDigest" lib/services/email.service.ts | awk '{ if ($1 < 7) { print "FAIL: only " $1 " methods found"; exit 1 } else { print "ok: " $1 " references" } }'</automated>
  </verify>
  <done>
    Seven new methods exist on `EmailService`, exported via the singleton; corresponding interfaces (`SellerDigestData`, `FinanceDigestData`, `AdminDigestData`) are exported; `tsc --noEmit` passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire real-time hook points (overdue cron + QB webhook + DocuSign webhook)</name>
  <files>app/api/cron/overdue-invoices/route.ts, app/api/webhooks/quickbooks/route.ts, app/api/webhooks/docusign/route.ts</files>
  <action>
    1) `app/api/cron/overdue-invoices/route.ts` — find the loop where invoices transition into OVERDUE. After successful update, if `invoice.ownerId` is set, fetch the owner User (`role: SALES`) and call `emailService.sendSellerInvoiceOverdue(invoice, owner)`. Skip if owner missing or role !== SALES. Wrap in try/catch — log and continue (do not fail the cron).

    2) `app/api/webhooks/quickbooks/route.ts` — locate the Payment / payment-event handler (search for "Payment" event type or where Invoice status is updated to PAID). After persisting the paid status, look up `Invoice` with `include: { owner: true, customer: true, deal: true }`. If `invoice.owner` exists and `role === 'SALES'`, call `emailService.sendSellerInvoicePaid(invoice, invoice.owner)`. Try/catch with structured log.

    3) `app/api/webhooks/docusign/route.ts` — for each envelope-status branch:
        * `completed` (signed): resolve contract -> deal -> `Deal.owner` (or fall back to Invoice.owner via `contract.invoiceId`). Call `emailService.sendSellerContractSigned(contract, seller, deal?.title)`. KEEP the existing `sendContractSigned` finance routing untouched (additive — both fire).
        * `declined` -> `sendSellerContractUnsigned(contract, seller, 'declined')`
        * `voided`   -> `sendSellerContractUnsigned(contract, seller, 'voided')`
       In `app/api/cron/contract-expiration/route.ts` (already exists per scope hint — read it first, then) when an envelope is marked expired, call `sendSellerContractUnsigned(contract, seller, 'expired')`.
       Skip cleanly when seller cannot be resolved.

    Each call site logs success/failure to console with prefix `[SellerNotify]` and never throws to the outer handler.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -l "sendSellerInvoiceOverdue" app/api/cron/overdue-invoices/route.ts && grep -l "sendSellerInvoicePaid" app/api/webhooks/quickbooks/route.ts && grep -l "sendSellerContract" app/api/webhooks/docusign/route.ts</automated>
  </verify>
  <done>
    Three hook points invoke the new seller methods; tsc passes; existing handler behavior preserved (additive only).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Build 3 digest cron routes + register in vercel.json</name>
  <files>app/api/cron/seller-digest/route.ts, app/api/cron/finance-digest/route.ts, app/api/cron/admin-digest/route.ts, vercel.json</files>
  <action>
    Use `app/api/cron/daily-ar-digest/route.ts` as the structural template (auth header check, force-dynamic, results array, summary log).

    1) `app/api/cron/seller-digest/route.ts` (POST):
       - Auth: `Bearer ${process.env.CRON_SECRET}`.
       - Query: `prisma.user.findMany({ where: { active: true, role: 'SALES' } })`.
       - For each seller, gather:
           overdueInvoices: `prisma.invoice.findMany({ where: { ownerId: seller.id, status: 'OVERDUE' }, include: { customer: true } })` -> map to `{invoiceNumber, customerName, amount: Number(amount), daysOverdue: differenceInDays(now, dueDate)}`.
           unsignedContracts: `prisma.contract.findMany({ where: { status: { in: ['SENT','DELIVERED'] }, sentAt: { lte: subDays(now, 7) }, deal: { ownerId: seller.id } }, include: { deal: true } })`.
           staleDeals: `prisma.deal.findMany({ where: { ownerId: seller.id, status: { notIn: ['WON','LOST'] }, updatedAt: { lte: subDays(now, 14) } } })` -> top 10 by value desc.
           unansweredLeads: `prisma.lead.findMany({ where: { assignedToId: seller.id, status: 'NEW', createdAt: { lte: subHours(now, 24) } } })` (adjust field names to actual schema if `assignedToId` differs — fall back to `ownerId`).
       - If all four arrays are empty, skip and log `Skipping ${seller.email}`.
       - Else `await emailService.sendSellerDailyDigest(seller, digestData)`.

    2) `app/api/cron/finance-digest/route.ts` (POST):
       - Recipients: `prisma.user.findMany({ where: { active: true, role: 'FINANCE' } })`.
       - Build a SINGLE shared `FinanceDigestData` (same data for every finance user — query once outside the user loop):
           arAging buckets: query overdue invoices grouped by `differenceInDays(now, dueDate)` ranges.
           todayExpectedCollections: invoices with `dueDate` in today's date range and status SENT.
           syncErrorInvoices: `prisma.integrationLog.findMany({ where: { service: 'quickbooks', status: 'ERROR', createdAt: { gte: subDays(now, 1) } }, take: 10 })` joined to invoice ids when present.
           cashflow: import `getFinancialKPIs` from `lib/services/financial-bi.ts` for thisMonth + lastMonth revenue.
           staleInvoices: count + sum where overdue 180+ days.
       - Send to each finance user via `sendFinanceDailyDigest`.

    3) `app/api/cron/admin-digest/route.ts` (POST):
       - Recipients: `prisma.user.findMany({ where: { active: true, role: 'ADMIN' } })`.
       - Build SINGLE shared `AdminDigestData`:
           mrr + delta: from `getFinancialKPIs` (week-over-week using current week vs previous week revenue).
           dealsAtRisk: `prisma.deal.findMany({ where: { status: { notIn: ['WON','LOST'] } }, orderBy: { value: 'desc' }, take: 5, include: { owner: true } })` filtered to deals with stale activity.
           cfoInsights: read latest CFO analysis. Probe order: (a) `prisma.cfoAnalysis.findFirst({ orderBy: { createdAt: 'desc' } })` if model exists; (b) most recent file under a `.cfo-cache/` dir; (c) fallback string `'Sem analises recentes do CFO IA.'`. Inspect schema/code at execution time and pick the right path.
           conversionFunnel: counts of leads, leads with status=QUALIFIED, deals created, deals won (current week).
           leadSourcePerformance: groupBy `Lead.source` -> { leads, conversions }.
           biHighlights: pull 3-5 strings from `getFinancialBIData()` summary.
       - Send weekly via `sendAdminWeeklyDigest`.

    4) `vercel.json` — append to the `crons` array (preserve existing entries):
       ```
       { "path": "/api/cron/seller-digest",  "schedule": "0 8 * * *" },
       { "path": "/api/cron/finance-digest", "schedule": "0 8 * * *" },
       { "path": "/api/cron/admin-digest",   "schedule": "0 8 * * 1" }
       ```

    All three cron routes export `const dynamic = "force-dynamic"` and return JSON `{ success, sent, failed, results }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const v=require('./vercel.json'); const paths=v.crons.map(c=>c.path); ['seller','finance','admin'].forEach(s=>{if(!paths.includes('/api/cron/'+s+'-digest'))throw new Error('missing '+s)}); console.log('ok')" && test -f app/api/cron/seller-digest/route.ts && test -f app/api/cron/finance-digest/route.ts && test -f app/api/cron/admin-digest/route.ts</automated>
  </verify>
  <done>
    Three cron route files exist, compile, are registered in `vercel.json` with the correct schedules; manual POST with valid `CRON_SECRET` would enumerate eligible users and call the matching new email method.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean across the touched files.
- `npx prisma validate` passes; `prisma generate` produced new enum values.
- `grep` confirms no legacy hex colors (`#2563eb`, `#10b981`, `#dc2626`, `#f59e0b`) remain in `lib/services/email.service.ts`.
- Manual smoke (optional, not blocking): trigger each new cron locally with `curl -X POST -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/{seller,finance,admin}-digest` and confirm Resend receives requests (or PENDING notifications row created when RESEND_API_KEY is unset).
- DocuSign + QB webhook hook points wrapped in try/catch — webhook handlers never throw on notification failure.
</verification>

<success_criteria>
- All 16+ existing email templates rendered via `renderBaseLayout` with Carreira USA palette + Arial fallback.
- 7 new `EmailService` methods exist, exported, type-safe.
- 3 new cron routes registered in vercel.json with correct schedules (0 8 * * *, 0 8 * * *, 0 8 * * 1).
- Real-time hooks fire on invoice overdue, invoice paid, contract signed, contract unsigned (declined/voided/expired) — additive to existing finance routing.
- Prisma `NotificationType` enum extended with 7 new values and `db:push` applied.
- Empty seller-digest sections do not produce sent email.
- Internal templates are PT-BR only; customer templates retain existing locale logic.
</success_criteria>

<output>
After completion, create `.planning/quick/260414-dg2-rebrand-emails-5-internal-notification-s/260414-dg2-SUMMARY.md` summarizing rebrand audit (which templates changed), new method names, hook-point file:line edits, cron schedules registered, and any deviations (e.g., if `CfoAnalysis` model didn't exist and a file fallback was used).
</output>
