---
phase: quick
plan: 260414-d0t
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/email.service.ts
  - lib/services/notification.service.ts
  - lib/services/email-service.ts
  - app/api/cron/daily-ar-digest/route.ts
  - app/api/cron/overdue-invoice-alerts/route.ts
  - app/api/public/checkout/route.ts
autonomous: true
requirements: [QUICK-EMAIL-CONSOLIDATION]
must_haves:
  truths:
    - "All email sending goes through a single core sendEmail method using Resend SDK"
    - "Circuit breaker protects all email sends from cascading Resend API failures"
    - "Integration logging tracks all email attempts in Notification table"
    - "All existing email templates (financial + contract + hub) still work with same public API"
    - "Dead code email-service.ts stub is removed"
  artifacts:
    - path: "lib/services/email.service.ts"
      provides: "Consolidated email service with Resend SDK, circuit breaker, DB logging, and all templates"
    - path: "lib/services/notification.service.ts"
      provides: "Thin re-export layer or updated imports pointing to consolidated service"
  key_links:
    - from: "lib/services/email.service.ts"
      to: "resend SDK"
      via: "getResendClient() lazy init"
      pattern: "new Resend"
    - from: "lib/services/email.service.ts"
      to: "prisma.notification"
      via: "DB logging on every send"
      pattern: "prisma\\.notification\\.create"
    - from: "app/api/cron/daily-ar-digest/route.ts"
      to: "lib/services/email.service.ts"
      via: "import emailService"
      pattern: "from.*email\\.service"
    - from: "lib/services/contract-workflow.service.ts"
      to: "lib/services/notification.service.ts"
      via: "import notificationService"
      pattern: "from.*notification\\.service"
---

<objective>
Consolidate three fragmented email implementations into a single centralized email service.

Purpose: Currently there are 3 email files -- `email.service.ts` (raw fetch, financial templates), `email-service.ts` (dead stub), and `notification.service.ts` (Resend SDK, circuit breaker, contract/hub templates). This creates inconsistent reliability: financial emails lack circuit breaker and DB logging while contract emails have both. Consolidating gives every email the same resilience.

Output: One `email.service.ts` with Resend SDK + circuit breaker + DB logging as the core sender, all templates preserved, dead code removed, all importers updated.
</objective>

<execution_context>
@.planning/quick/260414-d0t-add-reusable-email-system-using-resend/260414-d0t-PLAN.md
</execution_context>

<context>
@CLAUDE.md
@lib/services/email.service.ts
@lib/services/email-service.ts
@lib/services/notification.service.ts
@lib/utils/circuit-breaker.ts
@lib/utils/logger.ts

Importers to update:
- app/api/cron/daily-ar-digest/route.ts — imports { emailService, DailyDigestEmail }
- app/api/cron/overdue-invoice-alerts/route.ts — imports { emailService, OverdueInvoiceEmail }
- app/api/public/checkout/route.ts — imports { emailService }
- lib/services/contract-workflow.service.ts — imports { notificationService }
- app/api/webhooks/docusign/route.ts — imports { notificationService }
- lib/services/invoice-approval.service.ts — imports { notificationService }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild email.service.ts as the consolidated email service</name>
  <files>lib/services/email.service.ts, lib/services/email-service.ts</files>
  <action>
Rewrite `lib/services/email.service.ts` to be the single source of truth for all email sending. The new service must:

1. **Core infrastructure (adopt from notification.service.ts):**
   - Import `Resend` from 'resend' (SDK, not raw fetch)
   - Import `CircuitBreaker, CircuitOpenError` from '@/lib/utils/circuit-breaker'
   - Import `integrationLogger, StructuredErrorData` from '@/lib/utils/logger'
   - Import `prisma` from '@/lib/db'
   - Import `NotificationType, NotificationStatus` from '@prisma/client'
   - Lazy-init Resend client via `getResendClient()` (same pattern as notification.service.ts lines 8-15)
   - Constants: EMAIL_FROM, EMAIL_FINANCE_TEAM, EMAIL_SUPPORT_TEAM, APP_URL (same as notification.service.ts lines 17-20)

2. **Core sendEmail method (adopt from notification.service.ts lines 259-430):**
   - Private method: `sendEmail(to, subject, html, type: NotificationType, relations: { invoiceId?, contractId?, customerId? })`
   - Uses `this.circuitBreaker.execute()` to wrap Resend SDK calls
   - Logs to `prisma.notification.create()` on success (SENT) and failure (PENDING/FAILED)
   - Handles CircuitOpenError gracefully (log + track as PENDING, don't throw)
   - Uses integrationLogger for structured error logging
   - This replaces the old raw `fetch()` sendEmail method

3. **Keep ALL existing interfaces/types exported:**
   - `EmailTemplate`, `OverdueInvoiceEmail`, `DailyDigestEmail` (from current email.service.ts)
   - Add the Invoice, Contract, Customer, User interfaces (from notification.service.ts lines 22-62)

4. **Keep ALL financial template methods (from current email.service.ts):**
   - `sendOverdueInvoiceAlert(email, data)` — same signature, now calls new sendEmail with NotificationType.INVOICE_OVERDUE (or closest available enum value, check Prisma schema)
   - `sendDailyDigest(email, data)` — same signature
   - `sendStaleInvoiceAlert(email, userName, invoices)` — same signature
   - `sendWelcomeWithTempPassword(data)` — same signature
   - All HTML templates stay exactly the same

5. **Move ALL notification template methods (from notification.service.ts):**
   - `sendContractForSignature(contract, customer, invoiceNumber)`
   - `sendContractSigned(contract, customer)`
   - `sendContractExpired(contract, customer)`
   - `sendPaymentLink(invoice, customer, paymentUrl)`
   - `sendPaymentReminder(invoice, customer, paymentUrl)`
   - `sendPaymentReceived(invoice, customer)`
   - `sendHubWelcome(customer, tempPassword)`
   - `sendHubInvoiceAvailable(customer, invoice)`
   - `sendHubPasswordReset(customer, resetUrl)`
   - All private `generateXxxEmail()` HTML helper methods
   - Deprecated methods (sendInvoiceApprovalRequest, sendInvoiceApproved, sendInvoiceRejected) — keep as deprecated stubs

6. **Export TWO singletons for backward compatibility:**
   - `export const emailService = new EmailService();`
   - `export const notificationService = emailService;` (alias so existing imports don't break)

7. **Delete `lib/services/email-service.ts`** (the dead stub file). Use `git rm` or just delete it.

The class name stays `EmailService`. Constructor initializes CircuitBreaker("email").
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/carreirahubproject && npx tsc --noEmit lib/services/email.service.ts 2>&1 | head -30</automated>
  </verify>
  <done>email.service.ts compiles with Resend SDK, circuit breaker, DB logging, and all templates from both old services. email-service.ts deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Update notification.service.ts to re-export and fix all importers</name>
  <files>lib/services/notification.service.ts, app/api/cron/daily-ar-digest/route.ts, app/api/cron/overdue-invoice-alerts/route.ts, app/api/public/checkout/route.ts, lib/services/contract-workflow.service.ts, app/api/webhooks/docusign/route.ts, lib/services/invoice-approval.service.ts</files>
  <action>
1. **Replace `lib/services/notification.service.ts` entirely** with a thin re-export file:
   ```typescript
   /**
    * Notification Service — Re-export from consolidated email service
    * 
    * All email/notification functionality has been consolidated into email.service.ts.
    * This file exists for backward compatibility with existing imports.
    */
   export { emailService as notificationService, EmailService as NotificationService } from './email.service';
   ```
   This preserves all existing `import { notificationService } from './notification.service'` calls without changes needed.

2. **Verify these importers still work (no changes needed if re-export is correct):**
   - `lib/services/contract-workflow.service.ts` — imports `{ notificationService }`
   - `app/api/webhooks/docusign/route.ts` — imports `{ notificationService }`
   - `lib/services/invoice-approval.service.ts` — imports `{ notificationService }`

3. **Verify these importers still work (emailService export unchanged):**
   - `app/api/cron/daily-ar-digest/route.ts` — imports `{ emailService, DailyDigestEmail }`
   - `app/api/cron/overdue-invoice-alerts/route.ts` — imports `{ emailService, OverdueInvoiceEmail }`
   - `app/api/public/checkout/route.ts` — imports `{ emailService }`

4. **Also check for `invoice-approval.service.ts.disabled`** — if it exists and imports notification.service, leave it as-is (disabled file).

No import paths need changing in consumer files since both `emailService` and `notificationService` are exported from their original module paths.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/carreirahubproject && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <done>Full project compiles with zero type errors. notification.service.ts is a thin re-export. All 6+ consumer files resolve their imports correctly. No duplicate EmailService/NotificationService class definitions.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -r "from.*email-service" lib/ app/` returns no results (dead stub fully removed)
3. `grep -r "new Resend\|getResendClient" lib/services/` only appears in email.service.ts (single Resend client)
4. `grep -r "CircuitBreaker" lib/services/email.service.ts` confirms circuit breaker is present
5. `grep -r "prisma.notification.create" lib/services/email.service.ts` confirms DB logging is present
</verification>

<success_criteria>
- Single email.service.ts file contains ALL email sending logic with Resend SDK, circuit breaker, and DB logging
- notification.service.ts is a thin re-export (under 10 lines of actual code)
- email-service.ts (dead stub) is deleted
- All existing importers compile and function without import path changes
- Every email send (financial, contract, hub) goes through circuit-breaker-protected Resend SDK
</success_criteria>

<output>
After completion, create `.planning/quick/260414-d0t-add-reusable-email-system-using-resend/260414-d0t-SUMMARY.md`
</output>
