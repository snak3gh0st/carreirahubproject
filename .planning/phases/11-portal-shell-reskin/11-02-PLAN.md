---
phase: 11-portal-shell-reskin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/button.tsx
  - components/ui/badge.tsx
  - components/ui/stat-card.tsx
  - components/ui/input.tsx
  - app/layout.tsx
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - "Button primary variant uses Tangerina (#FF8142) background instead of gold-600"
    - "Button outline variant uses Tangerina border/text instead of gold-600"
    - "Badge info variant uses Creme bg + Verde text instead of gold-100/700"
    - "StatCard hover border uses Caramelo instead of gold-200"
    - "StatCard icon area uses Creme bg + Verde text instead of gold-50/600"
    - "Input default focus ring uses Verde instead of gold-500"
    - "Root layout skip-link uses Tangerina instead of gold-600"
  artifacts:
    - path: "components/ui/button.tsx"
      provides: "Brand-token button with Tangerina primary"
      contains: "brand-tangerina"
    - path: "components/ui/badge.tsx"
      provides: "Brand-token badge with Creme/Verde info variant"
      contains: "brand-creme"
    - path: "components/ui/stat-card.tsx"
      provides: "Brand-token stat card with Caramelo hover"
      contains: "brand-caramelo"
    - path: "components/ui/input.tsx"
      provides: "Brand-token input with Verde focus ring"
      contains: "brand-verde"
    - path: "app/layout.tsx"
      provides: "Skip-link with Tangerina focus styling"
      contains: "brand-tangerina"
  key_links:
    - from: "components/ui/button.tsx"
      to: "tailwind.config.ts"
      via: "brand-tangerina Tailwind class"
      pattern: "brand-tangerina"
    - from: "components/ui/stat-card.tsx"
      to: "tailwind.config.ts"
      via: "brand-caramelo and brand-verde Tailwind classes"
      pattern: "brand-caramelo|brand-verde"
---

<objective>
Migrate all shared UI components from legacy gold-* Tailwind classes to brand token equivalents.

Purpose: Shared components (Button, Badge, StatCard, Input) are used across both portals. Updating them to brand tokens ensures every page that uses these components automatically reflects the new Carreira USA visual identity without per-page changes.

Output: Five files updated with brand token classes replacing all gold-* references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-portal-shell-reskin/11-RESEARCH.md

<interfaces>
<!-- Exact Tailwind class replacements from research migration map -->

Button (components/ui/button.tsx):
  primary:  bg-gold-600 -> bg-brand-tangerina
            hover:bg-gold-700 -> hover:bg-brand-tangerina/90
            focus:ring-gold-500 -> focus:ring-brand-tangerina
  secondary: focus:ring-gold-500 -> focus:ring-brand-verde
  outline:  border-gold-600 -> border-brand-tangerina
            text-gold-600 -> text-brand-tangerina
            hover:bg-gold-50 -> hover:bg-brand-creme
            focus:ring-gold-500 -> focus:ring-brand-tangerina

Badge (components/ui/badge.tsx):
  info variant: bg-gold-100 -> bg-brand-creme
                text-gold-700 -> text-brand-verde
  info dot:     bg-gold-700 -> bg-brand-verde

StatCard (components/ui/stat-card.tsx):
  hover border: hover:border-gold-200 -> hover:border-brand-caramelo
  icon bg:      bg-gold-50 -> bg-brand-creme
  icon text:    text-gold-600 -> text-brand-verde

Input (components/ui/input.tsx):
  default focus: focus:ring-gold-500 -> focus:ring-brand-verde
                 focus:border-gold-500 -> focus:border-brand-verde

Root layout (app/layout.tsx):
  skip-link: focus:bg-gold-600 -> focus:bg-brand-tangerina
             focus:ring-gold-500 -> focus:ring-brand-tangerina
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Button, Badge, and Input from gold-* to brand tokens</name>
  <read_first>
    components/ui/button.tsx
    components/ui/badge.tsx
    components/ui/input.tsx
  </read_first>
  <files>components/ui/button.tsx, components/ui/badge.tsx, components/ui/input.tsx</files>
  <action>
**Button (components/ui/button.tsx) — per DASH-02:**

In the `variantStyles` object (lines 24-33), make these exact string replacements:

1. `primary` variant (line 25):
   - `bg-gold-600` -> `bg-brand-tangerina`
   - `hover:bg-gold-700` -> `hover:bg-brand-tangerina/90`
   - `focus:ring-gold-500` -> `focus:ring-brand-tangerina`
   - Full new string: `"bg-brand-tangerina text-white hover:bg-brand-tangerina/90 hover:-translate-y-0.5 hover:shadow-md active:translate-y-0 active:shadow-sm disabled:opacity-50 focus:ring-brand-tangerina shadow-sm"`

2. `secondary` variant (line 26):
   - `focus:ring-gold-500` -> `focus:ring-brand-verde`
   - Full new string: `"bg-white border border-gray-300 text-gray-700 hover:bg-gray-50 hover:-translate-y-0.5 hover:shadow-md active:translate-y-0 active:shadow-sm disabled:opacity-50 focus:ring-brand-verde"`

3. `outline` variant (line 27):
   - `border-gold-600` -> `border-brand-tangerina`
   - `text-gold-600` -> `text-brand-tangerina`
   - `hover:bg-gold-50` -> `hover:bg-brand-creme`
   - `focus:ring-gold-500` -> `focus:ring-brand-tangerina`
   - Full new string: `"bg-transparent border border-brand-tangerina text-brand-tangerina hover:bg-brand-creme hover:-translate-y-0.5 hover:shadow-md active:translate-y-0 active:shadow-sm disabled:opacity-50 focus:ring-brand-tangerina"`

Leave `ghost`, `destructive`, `success`, `danger` variants unchanged (they use gray/error/success tokens, not gold).

**Badge (components/ui/badge.tsx) — per DASH-02:**

1. In `variantStyles` (line 31):
   - `info` variant: `"bg-gold-100 text-gold-700"` -> `"bg-brand-creme text-brand-verde"`

2. In `dotStyles` (line 40):
   - `info` dot: `"bg-gold-700"` -> `"bg-brand-verde"`

Leave all other variants unchanged (success, error, warning use semantic tokens).

**Input (components/ui/input.tsx) — per DASH-02:**

In `stateStyles` (line 24):
- `default`: `"border-gray-300 focus:ring-gold-500 focus:border-gold-500"` -> `"border-gray-300 focus:ring-brand-verde focus:border-brand-verde"`

Leave `error` and `success` states unchanged.
  </action>
  <verify>
    <automated>grep -n 'gold-' components/ui/button.tsx components/ui/badge.tsx components/ui/input.tsx | wc -l | xargs test 0 -eq && grep -q 'brand-tangerina' components/ui/button.tsx && grep -q 'brand-creme' components/ui/badge.tsx && grep -q 'brand-verde' components/ui/input.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gold-' components/ui/button.tsx` returns 0
    - `grep -c 'gold-' components/ui/badge.tsx` returns 0
    - `grep -c 'gold-' components/ui/input.tsx` returns 0
    - `grep -c 'brand-tangerina' components/ui/button.tsx` returns at least 3 (primary bg, outline border, outline text)
    - `grep -c 'brand-creme' components/ui/button.tsx` returns at least 1 (outline hover)
    - `grep -c 'brand-verde' components/ui/button.tsx` returns at least 1 (secondary focus ring)
    - `grep -c 'brand-creme' components/ui/badge.tsx` returns at least 1
    - `grep -c 'brand-verde' components/ui/badge.tsx` returns at least 1
    - `grep -c 'brand-verde' components/ui/input.tsx` returns at least 2 (ring + border)
  </acceptance_criteria>
  <done>Button, Badge, and Input components use brand tokens; zero gold-* references remain in these files</done>
</task>

<task type="auto">
  <name>Task 2: Migrate StatCard and root layout skip-link from gold-* to brand tokens</name>
  <read_first>
    components/ui/stat-card.tsx
    app/layout.tsx
  </read_first>
  <files>components/ui/stat-card.tsx, app/layout.tsx</files>
  <action>
**StatCard (components/ui/stat-card.tsx) — per DASH-02:**

1. **Line 53 — hover border:**
   - REMOVE: `hover:border-gold-200`
   - ADD: `hover:border-brand-caramelo`

2. **Line 63 — icon background:**
   - REMOVE: `bg-gold-50`
   - ADD: `bg-brand-creme`

3. **Line 64 — icon text color:**
   - REMOVE: `text-gold-600`
   - ADD: `text-brand-verde`

**Root layout skip-link (app/layout.tsx) — per DASH-02:**

In `app/layout.tsx` lines 28-30, the skip-link anchor:
- REMOVE: `focus:bg-gold-600` -> ADD: `focus:bg-brand-tangerina`
- REMOVE: `focus:ring-gold-500` -> ADD: `focus:ring-brand-tangerina`

The full className string for the skip-link should become:
```
"sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4
 focus:z-50 focus:px-4 focus:py-2 focus:bg-brand-tangerina focus:text-white
 focus:rounded-md focus:shadow-lg focus:outline-none focus:ring-2
 focus:ring-brand-tangerina focus:ring-offset-2"
```
  </action>
  <verify>
    <automated>grep -n 'gold-' components/ui/stat-card.tsx app/layout.tsx | wc -l | xargs test 0 -eq && grep -q 'brand-caramelo' components/ui/stat-card.tsx && grep -q 'brand-creme' components/ui/stat-card.tsx && grep -q 'brand-verde' components/ui/stat-card.tsx && grep -q 'brand-tangerina' app/layout.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gold-' components/ui/stat-card.tsx` returns 0
    - `grep -c 'gold-' app/layout.tsx` returns 0
    - `grep -c 'brand-caramelo' components/ui/stat-card.tsx` returns 1
    - `grep -c 'brand-creme' components/ui/stat-card.tsx` returns 1
    - `grep -c 'brand-verde' components/ui/stat-card.tsx` returns 1
    - `grep -c 'brand-tangerina' app/layout.tsx` returns at least 2 (bg + ring)
  </acceptance_criteria>
  <done>StatCard uses brand-caramelo hover, brand-creme icon bg, brand-verde icon text; skip-link uses brand-tangerina; zero gold-* references remain in either file</done>
</task>

</tasks>

<verification>
```bash
# Complete gold-* elimination check across all 5 files
grep -n 'gold-' components/ui/button.tsx components/ui/badge.tsx components/ui/stat-card.tsx components/ui/input.tsx app/layout.tsx
# Expected: zero matches

# Brand tokens present
grep -n 'brand-' components/ui/button.tsx components/ui/badge.tsx components/ui/stat-card.tsx components/ui/input.tsx app/layout.tsx | head -20
# Expected: multiple matches across all files

# TypeScript check
npx tsc --noEmit --pretty 2>&1 | head -20
```
</verification>

<success_criteria>
1. Zero `gold-*` Tailwind classes remain across all 5 modified files
2. Button primary uses `bg-brand-tangerina`, outline uses `border-brand-tangerina text-brand-tangerina`
3. Badge info uses `bg-brand-creme text-brand-verde`
4. StatCard uses `hover:border-brand-caramelo`, `bg-brand-creme`, `text-brand-verde`
5. Input default focus uses `focus:ring-brand-verde focus:border-brand-verde`
6. Root skip-link uses `focus:bg-brand-tangerina focus:ring-brand-tangerina`
7. All non-gold variants (ghost, destructive, success, error, warning) remain unchanged
8. TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-portal-shell-reskin/11-02-SUMMARY.md`
</output>
