---
phase: 11-portal-shell-reskin
plan: 05
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - app/hub/status/page.tsx
  - app/hub/test/page.tsx
  - app/hub/test/result/page.tsx
  - app/hub/documents/page.tsx
  - app/hub/documents/receipt/[invoiceId]/page.tsx
  - app/hub/documents/receipt/PrintButton.tsx
autonomous: true
requirements: [HUB-01]

must_haves:
  truths:
    - "Hub status page has zero GOLD constants or #C9A84C hex values"
    - "Hub test page has zero GOLD constants or inline brand-color styles"
    - "Hub test result page uses success-600/error-600 for status colors, not raw hex"
    - "Hub documents page has zero GOLD references"
    - "Hub receipt page has zero GOLD references"
    - "PrintButton uses brand token instead of GOLD"
    - "After this plan, grep -rn 'GOLD|#C9A84C' app/hub/ returns ZERO matches across ALL hub files"
  artifacts:
    - path: "app/hub/status/page.tsx"
      provides: "Status page with brand tokens"
      contains: "brand-"
    - path: "app/hub/test/page.tsx"
      provides: "Test page with brand tokens"
      contains: "brand-"
    - path: "app/hub/test/result/page.tsx"
      provides: "Test result with semantic status colors"
      contains: "brand-"
    - path: "app/hub/documents/page.tsx"
      provides: "Documents page with brand tokens"
      contains: "brand-"
    - path: "app/hub/documents/receipt/[invoiceId]/page.tsx"
      provides: "Receipt page with brand tokens"
      contains: "brand-"
    - path: "app/hub/documents/receipt/PrintButton.tsx"
      provides: "Print button with brand token"
      contains: "brand-"
  key_links:
    - from: "app/hub/status/page.tsx"
      to: "lib/constants/brand.ts"
      via: "import { BRAND_COLORS }"
      pattern: "BRAND_COLORS"
    - from: "app/hub/test/page.tsx"
      to: "lib/constants/brand.ts"
      via: "import { BRAND_COLORS }"
      pattern: "BRAND_COLORS"
---

<objective>
Migrate remaining hub pages from GOLD constants to brand tokens (batch 2 of 2), completing the full hub GOLD elimination.

Purpose: This is the final batch of hub file migrations. After this plan, zero GOLD constants or #C9A84C hex values will remain anywhere in `app/hub/`. Combined with Plan 04, this fully satisfies HUB-01 (all hardcoded hex literals replaced with token classes).

Output: 6 hub files migrated, completing the GOLD elimination across the entire Client Hub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-portal-shell-reskin/11-RESEARCH.md

<interfaces>
<!-- Same GOLD elimination pattern as Plan 04 -->

Decision rule for each GOLD usage:

| Current usage | Replace with |
|---------------|-------------|
| style={{ backgroundColor: GOLD }} static | className="bg-brand-tangerina" |
| style={{ color: GOLD }} static on dark bg | className="text-brand-tangerina" |
| style={{ color: GOLD }} static on light bg | className="text-brand-verde" (WCAG) |
| style={{ borderColor: GOLD }} | className="border-brand-tangerina" |
| Conditional ternary with GOLD | BRAND_COLORS.TANGERINA |
| onFocus handler using GOLD | BRAND_COLORS.TANGERINA |
| style={{ backgroundColor: "#FBF8F0" }} | className="bg-brand-creme" |
| #059669 (green status) | bg-success-600 text-white |
| #DC2626 (red status) | bg-error-600 text-white |

From lib/constants/brand.ts:
```typescript
import { BRAND_COLORS } from "@/lib/constants/brand";
// BRAND_COLORS.TANGERINA = "#FF8142"
// BRAND_COLORS.VERDE = "#2F443F"
// BRAND_COLORS.CREME = "#FFF8E8"
```

Open question from research resolved: Status hex colors #059669 (green) and #DC2626 (red) in test/result/page.tsx SHOULD be replaced with bg-success-600 and bg-error-600 — both defined in tailwind.config.ts and consistent with Badge component approach.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate status page and test pages from GOLD to brand tokens</name>
  <read_first>
    app/hub/status/page.tsx
    app/hub/test/page.tsx
    app/hub/test/result/page.tsx
  </read_first>
  <files>app/hub/status/page.tsx, app/hub/test/page.tsx, app/hub/test/result/page.tsx</files>
  <action>
**Status page — app/hub/status/page.tsx (6 GOLD usages):**

1. DELETE `const GOLD = "#C9A84C";` declaration.
2. ADD `import { BRAND_COLORS } from "@/lib/constants/brand";` (only if conditionals exist).
3. For each of the 6 usages:
   - Static backgrounds on buttons/badges: `className="bg-brand-tangerina"`
   - Static borders: `className="border-brand-tangerina"`
   - Conditional ternaries: `BRAND_COLORS.TANGERINA`
   - Icon colors: `className="text-brand-tangerina"` on dark surfaces, `className="text-brand-verde"` on light
   - Any hex backgrounds (#FBF8F0, #FFF8E7): `className="bg-brand-creme"`
4. VERIFY: `grep -n 'GOLD\|#C9A84C' app/hub/status/page.tsx` returns zero.

**Test page — app/hub/test/page.tsx (11 GOLD usages):**

1. DELETE `const GOLD = "#C9A84C";` declaration.
2. ADD `import { BRAND_COLORS } from "@/lib/constants/brand";`.
3. This file has many usages — systematically replace each:
   - Static `style={{ backgroundColor: GOLD }}` on buttons/elements: `className="bg-brand-tangerina"`
   - Static `style={{ color: GOLD }}` on icons: depends on background context
   - Conditional styling in ternaries: `BRAND_COLORS.TANGERINA`
   - `onFocus/onBlur` handlers: `BRAND_COLORS.TANGERINA`
   - Any hardcoded hex backgrounds: `className="bg-brand-creme"`
   - Text on light (white/Creme) backgrounds: use `text-brand-verde`, NOT `text-brand-tangerina`
4. Also check for any `#FBF8F0` or `#FFF8E7` hex colors and replace with `bg-brand-creme`.
5. VERIFY: `grep -n 'GOLD\|#C9A84C\|#FBF8F0\|#FFF8E7' app/hub/test/page.tsx` returns zero.

**Test result page — app/hub/test/result/page.tsx (2 GOLD + 2 status hex):**

1. DELETE `const GOLD = "#C9A84C";` declaration.
2. For the 2 GOLD usages: apply standard replacement.
3. For the status hex colors (per research open question #3):
   - `#059669` (green/pass) -> replace with `bg-success-600` Tailwind class (already defined in tailwind.config.ts)
   - `#DC2626` (red/fail) -> replace with `bg-error-600` Tailwind class
   These are SEMANTIC status colors, not brand colors, so using the existing semantic tokens is correct.
4. VERIFY: `grep -n 'GOLD\|#C9A84C\|#059669\|#DC2626' app/hub/test/result/page.tsx` returns zero.
  </action>
  <verify>
    <automated>grep -rn 'GOLD\|#C9A84C' app/hub/status/page.tsx app/hub/test/page.tsx app/hub/test/result/page.tsx | wc -l | xargs test 0 -eq && grep -rn '#059669\|#DC2626' app/hub/test/result/page.tsx | wc -l | xargs test 0 -eq && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const GOLD' app/hub/status/page.tsx` returns 0
    - `grep -c 'const GOLD' app/hub/test/page.tsx` returns 0
    - `grep -c 'const GOLD' app/hub/test/result/page.tsx` returns 0
    - `grep -c '#C9A84C' app/hub/status/page.tsx` returns 0
    - `grep -c '#C9A84C' app/hub/test/page.tsx` returns 0
    - `grep -c '#C9A84C' app/hub/test/result/page.tsx` returns 0
    - `grep -c '#059669' app/hub/test/result/page.tsx` returns 0 (replaced with success-600)
    - `grep -c '#DC2626' app/hub/test/result/page.tsx` returns 0 (replaced with error-600)
    - Brand tokens (BRAND_COLORS or brand-* classes) present in all 3 files
  </acceptance_criteria>
  <done>Status, test, and test result pages use brand tokens; status hex colors replaced with semantic Tailwind tokens; zero GOLD/hex brand colors remain</done>
</task>

<task type="auto">
  <name>Task 2: Migrate documents pages and PrintButton from GOLD to brand tokens</name>
  <read_first>
    app/hub/documents/page.tsx
    app/hub/documents/receipt/[invoiceId]/page.tsx
    app/hub/documents/receipt/PrintButton.tsx
  </read_first>
  <files>app/hub/documents/page.tsx, app/hub/documents/receipt/[invoiceId]/page.tsx, app/hub/documents/receipt/PrintButton.tsx</files>
  <action>
**Documents listing — app/hub/documents/page.tsx (5 GOLD usages):**

1. DELETE `const GOLD = "#C9A84C";` declaration.
2. ADD `import { BRAND_COLORS } from "@/lib/constants/brand";` if any conditional usages.
3. Replace all 5 usages:
   - Static backgrounds: `className="bg-brand-tangerina"`
   - Static borders: `className="border-brand-tangerina"`
   - Static icon colors: `className="text-brand-tangerina"` (dark bg) or `text-brand-verde` (light bg)
   - Conditional: `BRAND_COLORS.TANGERINA`
   - Any hex background: `className="bg-brand-creme"`
4. VERIFY: `grep -n 'GOLD\|#C9A84C' app/hub/documents/page.tsx` returns zero.

**Receipt page — app/hub/documents/receipt/[invoiceId]/page.tsx (2 GOLD usages):**

1. DELETE `const GOLD = "#C9A84C";` declaration.
2. Replace 2 usages per decision table.
3. VERIFY: `grep -n 'GOLD\|#C9A84C' app/hub/documents/receipt/[invoiceId]/page.tsx` returns zero.

**PrintButton — app/hub/documents/receipt/PrintButton.tsx (2 GOLD usages):**

1. DELETE `const GOLD = "#C9A84C";` declaration (if present; file is only 17 lines).
2. Replace usages — likely a single static style on the button:
   - `style={{ backgroundColor: GOLD }}` or `style={{ color: GOLD }}` -> Tailwind class
3. VERIFY: `grep -n 'GOLD\|#C9A84C' app/hub/documents/receipt/PrintButton.tsx` returns zero.

**FINAL PHASE GATE — run after this task:**
```bash
grep -rn 'GOLD\|#C9A84C\|#c9a84c' app/hub/ --include="*.tsx"
```
This MUST return zero matches. This confirms HUB-01 is fully satisfied across ALL 15+ hub files.
  </action>
  <verify>
    <automated>grep -rn 'GOLD\|#C9A84C\|#c9a84c' app/hub/ --include="*.tsx" | wc -l | xargs test 0 -eq && echo "PHASE GATE PASS: zero GOLD in hub" || echo "PHASE GATE FAIL: GOLD still exists"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const GOLD' app/hub/documents/page.tsx` returns 0
    - `grep -c 'const GOLD' app/hub/documents/receipt/[invoiceId]/page.tsx` returns 0
    - `grep -c 'const GOLD' app/hub/documents/receipt/PrintButton.tsx` returns 0
    - `grep -c '#C9A84C' app/hub/documents/page.tsx` returns 0
    - `grep -c '#C9A84C' app/hub/documents/receipt/[invoiceId]/page.tsx` returns 0
    - `grep -c '#C9A84C' app/hub/documents/receipt/PrintButton.tsx` returns 0
    - PHASE GATE: `grep -rn 'GOLD\|#C9A84C\|#c9a84c' app/hub/ --include="*.tsx"` returns ZERO matches across the ENTIRE hub directory
    - TypeScript compilation passes: `npx tsc --noEmit`
  </acceptance_criteria>
  <done>All hub document pages use brand tokens; PHASE GATE PASSED — zero GOLD constants or #C9A84C hex values remain anywhere in app/hub/</done>
</task>

</tasks>

<verification>
```bash
# PHASE-LEVEL GATE: Complete hub GOLD elimination
grep -rn 'GOLD\|#C9A84C\|#c9a84c' app/hub/ --include="*.tsx"
# Expected: ZERO matches — this is THE gate check for HUB-01

# Inline brand-color style check
grep -rn 'style={{.*backgroundColor.*#\|style={{.*color.*#' app/hub/ --include="*.tsx" | grep -v 'BRAND_COLORS\|success\|error\|warning\|gray\|#E5E7EB\|#6B7280\|#374151\|#9CA3AF\|#D1D5DB'
# Expected: zero matches for brand-specific inline hex (utility colors like gray are OK)

# TypeScript check
npx tsc --noEmit --pretty 2>&1 | head -20

# Build check (final)
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
1. Zero `const GOLD = "#C9A84C"` declarations across entire `app/hub/` directory
2. Zero `#C9A84C` hex literals across entire `app/hub/` directory
3. Status colors in test/result use `bg-success-600` / `bg-error-600` (semantic tokens)
4. Dynamic/conditional usages use `BRAND_COLORS.TANGERINA` from shared module
5. Static usages use Tailwind `bg-brand-tangerina`, `border-brand-tangerina`, etc.
6. No `text-brand-tangerina` on Creme/white backgrounds
7. TypeScript compilation passes
8. Build passes (npm run build)
</success_criteria>

<output>
After completion, create `.planning/phases/11-portal-shell-reskin/11-05-SUMMARY.md`
</output>
