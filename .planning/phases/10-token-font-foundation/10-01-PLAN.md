---
phase: 10-token-font-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/fonts/blaak/ (12 OTF files)
  - public/fonts/neue-montreal/ (8 OTF files)
  - lib/fonts.ts
  - lib/constants/brand.ts
  - styles/tokens/brand.css
  - styles/tokens/semantic.css
  - styles/tokens/portal-dashboard.css
  - styles/tokens/portal-hub.css
autonomous: true
requirements: [TKN-01, TKN-02, TKN-04]

must_haves:
  truths:
    - "All five brand colors (Verde, Tangerina, Creme, Cafe, Caramelo) are defined as CSS custom properties in a single :root block"
    - "Semantic role aliases (--color-primary, --color-accent, --color-surface-base) map to brand primitives"
    - "Portal-scoped overrides exist for dashboard and hub via [data-portal] selectors"
    - "Blaak and Neue Montreal font families are defined as next/font/local instances with CSS variable output"
    - "A JS constants file exports all five brand hex values with TypeScript const assertions"
    - "Contrast safety rules are documented in the brand constants file"
  artifacts:
    - path: "styles/tokens/brand.css"
      provides: "Raw brand palette CSS custom properties"
      contains: "--brand-verde"
    - path: "styles/tokens/semantic.css"
      provides: "Role-based semantic aliases"
      contains: "--color-primary"
    - path: "styles/tokens/portal-dashboard.css"
      provides: "Dashboard portal CSS overrides"
      contains: "data-portal"
    - path: "styles/tokens/portal-hub.css"
      provides: "Hub portal CSS overrides"
      contains: "data-portal"
    - path: "lib/fonts.ts"
      provides: "Font definitions for Blaak and Neue Montreal"
      exports: ["blaak", "neueMontreal"]
    - path: "lib/constants/brand.ts"
      provides: "JS brand color constants and contrast safety rules"
      exports: ["BRAND_COLORS", "BrandColor", "BRAND_COLOR_SAFE_USE"]
    - path: "public/fonts/blaak/Blaak Regular.otf"
      provides: "Blaak font binary"
    - path: "public/fonts/neue-montreal/NeueMontreal-Regular.otf"
      provides: "Neue Montreal font binary"
  key_links:
    - from: "styles/tokens/semantic.css"
      to: "styles/tokens/brand.css"
      via: "var() references to brand primitives"
      pattern: "var\\(--brand-"
    - from: "lib/fonts.ts"
      to: "public/fonts/"
      via: "relative path references in src arrays"
      pattern: "public/fonts/"
---

<objective>
Create all foundation files for the brand token system and font infrastructure.

Purpose: Every subsequent plan and phase depends on these files existing. The brand CSS token hierarchy, font definitions, and JS constants must all be created before any existing files are modified. This is the "contracts first" commit.

Output: 6 new source files (4 CSS token files, 1 font definition module, 1 JS constants module) plus 20 font binary files copied into public/fonts/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-token-font-foundation/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy font files and create font definition module + brand constants</name>
  <files>
    public/fonts/blaak/ (12 OTF files)
    public/fonts/neue-montreal/ (8 OTF files)
    lib/fonts.ts
    lib/constants/brand.ts
  </files>
  <read_first>
    app/layout.tsx
    tailwind.config.ts
  </read_first>
  <action>
    **Step 1: Create directories and copy font files.**

    Create directories:
    - `public/fonts/blaak/`
    - `public/fonts/neue-montreal/`

    Copy all 12 Blaak OTF files from `/Users/pauloloureiro/Downloads/1. ID VISUAL - Carreira USA/Tipografia/Blaak_Font/Blaak/` to `public/fonts/blaak/`.

    Copy all 8 Neue Montreal OTF files from `/Users/pauloloureiro/Downloads/1. ID VISUAL - Carreira USA/Tipografia/Neue Montreal/` to `public/fonts/neue-montreal/`.

    **Step 2: Create `lib/fonts.ts`.**

    This file defines two `localFont` instances using `next/font/local`. Each uses a `src` array mapping weight+style to the OTF file. Each outputs a CSS variable via the `variable` option.

    ```typescript
    import localFont from 'next/font/local'

    export const blaak = localFont({
      src: [
        { path: '../public/fonts/blaak/Blaak Thin.otf',              weight: '100', style: 'normal' },
        { path: '../public/fonts/blaak/Blaak Thin Italic.otf',       weight: '100', style: 'italic' },
        { path: '../public/fonts/blaak/Blaak Light.otf',             weight: '300', style: 'normal' },
        { path: '../public/fonts/blaak/Blaak Light Italic.otf',      weight: '300', style: 'italic' },
        { path: '../public/fonts/blaak/Blaak Regular.otf',           weight: '400', style: 'normal' },
        { path: '../public/fonts/blaak/Blaak Regular Italic.otf',    weight: '400', style: 'italic' },
        { path: '../public/fonts/blaak/Blaak Bold.otf',              weight: '700', style: 'normal' },
        { path: '../public/fonts/blaak/Blaak Bold Italic.otf',       weight: '700', style: 'italic' },
        { path: '../public/fonts/blaak/Blaak ExtraBold.otf',         weight: '800', style: 'normal' },
        { path: '../public/fonts/blaak/Blaak ExtraBold Italic.otf',  weight: '800', style: 'italic' },
        { path: '../public/fonts/blaak/Blaak Black.otf',             weight: '900', style: 'normal' },
        { path: '../public/fonts/blaak/Blaak Black Italic.otf',      weight: '900', style: 'italic' },
      ],
      variable: '--font-blaak',
      display: 'swap',
      adjustFontFallback: 'Times New Roman',
    })

    export const neueMontreal = localFont({
      src: [
        { path: '../public/fonts/neue-montreal/NeueMontreal-Light.otf',        weight: '300', style: 'normal' },
        { path: '../public/fonts/neue-montreal/NeueMontreal-LightItalic.otf',  weight: '300', style: 'italic' },
        { path: '../public/fonts/neue-montreal/NeueMontreal-Regular.otf',      weight: '400', style: 'normal' },
        { path: '../public/fonts/neue-montreal/NeueMontreal-Italic.otf',       weight: '400', style: 'italic' },
        { path: '../public/fonts/neue-montreal/NeueMontreal-Medium.otf',       weight: '500', style: 'normal' },
        { path: '../public/fonts/neue-montreal/NeueMontreal-MediumItalic.otf', weight: '500', style: 'italic' },
        { path: '../public/fonts/neue-montreal/NeueMontreal-Bold.otf',         weight: '700', style: 'normal' },
        { path: '../public/fonts/neue-montreal/NeueMontreal-BoldItalic.otf',   weight: '700', style: 'italic' },
      ],
      variable: '--font-neue-montreal',
      display: 'swap',
      adjustFontFallback: 'Arial',
    })
    ```

    **Step 3: Create `lib/constants/brand.ts`.**

    Create the `lib/constants/` directory if it does not exist. Then create `brand.ts` with:

    ```typescript
    // Carreira USA official color guide — Paleta de Cores Carreira USA.pdf
    // This is the canonical JS source for brand colors.
    // Mirrors styles/tokens/brand.css primitive values.
    // Use for: Recharts props, dynamic style objects, any non-CSS consumer.
    // NEVER hardcode brand hex values anywhere else in the codebase.

    export const BRAND_COLORS = {
      CREME:     '#FFF8E8',
      VERDE:     '#2F443F',
      TANGERINA: '#FF8142',
      CAFE:      '#E1C19B',
      CARAMELO:  '#BD925F',
    } as const

    export type BrandColor = typeof BRAND_COLORS[keyof typeof BRAND_COLORS]

    /**
     * Contrast safety rules (WCAG AA at 4.5:1 for normal text):
     * - VERDE on white:      ~7.5:1  PASS
     * - VERDE on CREME:      ~7.2:1  PASS
     * - TANGERINA on white:  ~2.9:1  FAIL — never use as text on light bg
     * - TANGERINA on VERDE:  ~3.2:1  FAIL for normal text, borderline large text
     * - CREME on VERDE:      ~7.2:1  PASS — use for text in Verde header/sidebar
     * - CAFE on white:       ~2.6:1  FAIL — decorative only, never text
     * - CARAMELO on white:   ~3.1:1  FAIL for normal text — decorative only
     */
    export const BRAND_COLOR_SAFE_USE = {
      TEXT_ON_LIGHT:  [BRAND_COLORS.VERDE],
      TEXT_ON_DARK:   [BRAND_COLORS.CREME, BRAND_COLORS.TANGERINA],
      DECORATIVE_ONLY: [BRAND_COLORS.CAFE, BRAND_COLORS.CARAMELO],
      ACCENT_ON_DARK_ONLY: [BRAND_COLORS.TANGERINA],
    } as const
    ```

    Per TKN-04: The contrast safety comments serve as the codified contrast rules. Verde (#2F443F) is the safe primary text color on light backgrounds. Tangerina (#FF8142) must ONLY be used on dark surfaces (Verde background) or as a non-text decorative accent.
  </action>
  <verify>
    <automated>ls public/fonts/blaak/*.otf | wc -l && ls public/fonts/neue-montreal/*.otf | wc -l && grep "BRAND_COLORS" lib/constants/brand.ts && grep "localFont" lib/fonts.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/fonts/blaak/*.otf | wc -l` returns 12
    - `ls public/fonts/neue-montreal/*.otf | wc -l` returns 8
    - `grep "BRAND_COLORS" lib/constants/brand.ts` returns a match containing `CREME`, `VERDE`, `TANGERINA`, `CAFE`, `CARAMELO`
    - `grep "'#FFF8E8'" lib/constants/brand.ts` returns a match
    - `grep "'#2F443F'" lib/constants/brand.ts` returns a match
    - `grep "'#FF8142'" lib/constants/brand.ts` returns a match
    - `grep "'#E1C19B'" lib/constants/brand.ts` returns a match
    - `grep "'#BD925F'" lib/constants/brand.ts` returns a match
    - `grep "BRAND_COLOR_SAFE_USE" lib/constants/brand.ts` returns a match
    - `grep "as const" lib/constants/brand.ts` returns at least 2 matches
    - `grep "export type BrandColor" lib/constants/brand.ts` returns a match
    - `grep "localFont" lib/fonts.ts` returns at least 2 matches (one per font)
    - `grep "'--font-blaak'" lib/fonts.ts` returns a match
    - `grep "'--font-neue-montreal'" lib/fonts.ts` returns a match
    - `grep "display: 'swap'" lib/fonts.ts` returns at least 2 matches
    - `grep "Blaak Regular.otf" lib/fonts.ts` returns a match
    - `grep "NeueMontreal-Regular.otf" lib/fonts.ts` returns a match
  </acceptance_criteria>
  <done>
    - 12 Blaak OTF files exist in public/fonts/blaak/
    - 8 Neue Montreal OTF files exist in public/fonts/neue-montreal/
    - lib/fonts.ts exports `blaak` and `neueMontreal` localFont instances with CSS variable output (--font-blaak, --font-neue-montreal)
    - lib/constants/brand.ts exports BRAND_COLORS with all 5 hex values, BrandColor type, and BRAND_COLOR_SAFE_USE contrast rules
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CSS token hierarchy (brand primitives, semantic aliases, portal overrides)</name>
  <files>
    styles/tokens/brand.css
    styles/tokens/semantic.css
    styles/tokens/portal-dashboard.css
    styles/tokens/portal-hub.css
  </files>
  <read_first>
    app/globals.css
    lib/constants/brand.ts
  </read_first>
  <action>
    Create the `styles/tokens/` directory at the project root.

    **File 1: `styles/tokens/brand.css` (Layer 1 — Primitives)**

    Define raw brand palette values. These are the five official Carreira USA colors plus any tint stops needed for hover/active states.

    ```css
    /* styles/tokens/brand.css — Layer 1: Brand Primitives
       Source: Carreira USA official color guide (Paleta de Cores Carreira USA.pdf)
       These are the canonical CSS source for brand colors.
       Mirrors lib/constants/brand.ts for non-CSS consumers. */

    :root {
      /* === Brand Palette === */
      --brand-creme:        #FFF8E8;
      --brand-verde:        #2F443F;
      --brand-tangerina:    #FF8142;
      --brand-cafe:         #E1C19B;
      --brand-caramelo:     #BD925F;

      /* === Verde scale (approximate — verify via tints.dev before Phase 11) === */
      --brand-verde-50:     #EDF2F1;
      --brand-verde-100:    #CCDAD7;
      --brand-verde-200:    #AABFBA;
      --brand-verde-300:    #7FA09A;
      --brand-verde-400:    #587A73;
      --brand-verde-500:    #2F443F;
      --brand-verde-600:    #273A36;
      --brand-verde-700:    #1F302C;
      --brand-verde-800:    #1A2724;
      --brand-verde-900:    #131D1B;

      /* === Tangerina scale (approximate — verify via tints.dev before Phase 11) === */
      --brand-tangerina-50:  #FFF0E8;
      --brand-tangerina-100: #FFD5BC;
      --brand-tangerina-200: #FFBA90;
      --brand-tangerina-300: #FF9E64;
      --brand-tangerina-400: #FF8142;
      --brand-tangerina-500: #E66E34;
      --brand-tangerina-600: #CC5C28;
    }
    ```

    **File 2: `styles/tokens/semantic.css` (Layer 2 — Semantic Aliases)**

    Assign role-based names that map to brand primitives. Components use semantic names; they never reference primitives directly.

    ```css
    /* styles/tokens/semantic.css — Layer 2: Semantic Role Aliases
       Maps brand primitives to functional roles.
       Components should reference these, not brand.css primitives. */

    :root {
      /* === Color roles === */
      --color-primary:          var(--brand-verde);
      --color-accent:           var(--brand-tangerina);
      --color-surface-base:     var(--brand-creme);
      --color-surface-warm:     var(--brand-cafe);
      --color-surface-mid:      var(--brand-caramelo);

      /* === Text roles === */
      --color-text-primary:     var(--brand-verde);
      --color-text-on-dark:     var(--brand-creme);
      --color-text-accent:      var(--brand-tangerina);  /* ONLY on dark backgrounds */

      /* === Interactive roles === */
      --color-interactive:      var(--brand-verde);
      --color-interactive-hover: var(--brand-verde-700);
      --color-accent-hover:     var(--brand-tangerina-500);

      /* === Surface roles === */
      --color-surface-page:     var(--brand-creme);       /* Default — portals override */
      --color-sidebar-bg:       var(--brand-verde);
      --color-sidebar-active:   var(--brand-tangerina);
    }
    ```

    **File 3: `styles/tokens/portal-dashboard.css` (Layer 3 — Dashboard overrides)**

    ```css
    /* styles/tokens/portal-dashboard.css — Layer 3: Dashboard Portal Overrides
       Applied via [data-portal="dashboard"] on the dashboard layout wrapper.
       Dashboard keeps gray-50 page background (not Creme). */

    [data-portal="dashboard"] {
      --color-surface-page:     #F9FAFB;  /* gray-50 — dashboard stays neutral */
      --color-sidebar-bg:       var(--brand-verde);
      --color-sidebar-active:   var(--brand-tangerina);
    }
    ```

    **File 4: `styles/tokens/portal-hub.css` (Layer 3 — Hub overrides)**

    ```css
    /* styles/tokens/portal-hub.css — Layer 3: Hub Portal Overrides
       Applied via [data-portal="hub"] on the hub layout wrapper.
       Hub uses Creme as page background, Verde as primary text. */

    [data-portal="hub"] {
      --color-surface-page:     var(--brand-creme);
      --color-text-primary:     var(--brand-verde);
    }
    ```

    Per TKN-01: This three-layer architecture (primitives -> semantic -> portal overrides) is the single source of truth. Tailwind utility classes (Plan 02) will reference these CSS variables via `var()`.
  </action>
  <verify>
    <automated>grep '\-\-brand-verde' styles/tokens/brand.css && grep '\-\-color-primary' styles/tokens/semantic.css && grep 'data-portal' styles/tokens/portal-dashboard.css && grep 'data-portal' styles/tokens/portal-hub.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep '#FFF8E8' styles/tokens/brand.css` returns a match for --brand-creme
    - `grep '#2F443F' styles/tokens/brand.css` returns a match for --brand-verde
    - `grep '#FF8142' styles/tokens/brand.css` returns a match for --brand-tangerina
    - `grep '#E1C19B' styles/tokens/brand.css` returns a match for --brand-cafe
    - `grep '#BD925F' styles/tokens/brand.css` returns a match for --brand-caramelo
    - `grep '\-\-brand-verde-50' styles/tokens/brand.css` returns a match (verde scale exists)
    - `grep '\-\-brand-tangerina-50' styles/tokens/brand.css` returns a match (tangerina scale exists)
    - `grep 'var(--brand-verde)' styles/tokens/semantic.css` returns a match (semantic references primitive)
    - `grep '\-\-color-primary' styles/tokens/semantic.css` returns a match
    - `grep '\-\-color-accent' styles/tokens/semantic.css` returns a match
    - `grep '\-\-color-surface-base' styles/tokens/semantic.css` returns a match
    - `grep '\-\-color-text-primary' styles/tokens/semantic.css` returns a match
    - `grep '\-\-color-text-on-dark' styles/tokens/semantic.css` returns a match
    - `grep 'data-portal="dashboard"' styles/tokens/portal-dashboard.css` returns a match
    - `grep 'data-portal="hub"' styles/tokens/portal-hub.css` returns a match
    - `grep '#F9FAFB' styles/tokens/portal-dashboard.css` returns a match (dashboard keeps gray-50)
    - `grep 'var(--brand-creme)' styles/tokens/portal-hub.css` returns a match (hub uses Creme)
  </acceptance_criteria>
  <done>
    - brand.css defines all 5 brand hex values plus verde and tangerina tint scales as CSS custom properties in :root
    - semantic.css maps brand primitives to role-based aliases (--color-primary, --color-accent, --color-surface-base, etc.)
    - portal-dashboard.css scopes dashboard overrides via [data-portal="dashboard"]
    - portal-hub.css scopes hub overrides via [data-portal="hub"]
    - All four files exist in styles/tokens/ directory
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls public/fonts/blaak/*.otf | wc -l` returns 12
2. `ls public/fonts/neue-montreal/*.otf | wc -l` returns 8
3. `grep "BRAND_COLORS" lib/constants/brand.ts` returns match
4. `grep "localFont" lib/fonts.ts` returns match
5. `ls styles/tokens/*.css | wc -l` returns 4
6. `grep '\-\-brand-verde' styles/tokens/brand.css` returns match
7. `grep '\-\-color-primary' styles/tokens/semantic.css` returns match
</verification>

<success_criteria>
- All 20 font files present in public/fonts/ (12 Blaak + 8 Neue Montreal)
- lib/fonts.ts exists with two localFont exports using CSS variable output
- lib/constants/brand.ts exists with BRAND_COLORS, BrandColor type, and BRAND_COLOR_SAFE_USE
- 4 CSS token files exist in styles/tokens/ forming the three-layer hierarchy
- No existing files have been modified yet (that is Plan 02's job)
</success_criteria>

<output>
After completion, create `.planning/phases/10-token-font-foundation/10-01-SUMMARY.md`
</output>
