---
phase: 09-professional-ui-ux-enhancement
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - components/ui/button.tsx
  - components/ui/badge.tsx
  - components/ui/input.tsx
  - components/ui/card.tsx
  - components/ui/stat-card.tsx
  - components/ui/currency-input.tsx
  - components/ui/empty-state.tsx
  - components/ui/skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Buttons render in 5 variants (primary, secondary, outline, ghost, destructive) with proper design token colors"
    - "Buttons render in 5 sizes (xs, sm, md, lg, xl) with correct heights"
    - "Status badges show correct semantic colors (success, warning, error, info)"
    - "StatCard displays KPI data with trend indicators and proper typography"
    - "CurrencyInput formats numbers with $ prefix and comma separators"
    - "EmptyState component guides users with icon, message, and CTA"
    - "Skeleton loaders provide shimmer effect during loading"
  artifacts:
    - path: "components/ui/button.tsx"
      provides: "Enhanced button with 5 variants, 5 sizes, loading states"
      min_lines: 100
    - path: "components/ui/badge.tsx"
      provides: "Status badge with semantic colors from design tokens"
      min_lines: 50
    - path: "components/ui/stat-card.tsx"
      provides: "KPI card with label, value, trend indicator"
      min_lines: 80
    - path: "components/ui/currency-input.tsx"
      provides: "Formatted currency input with $ prefix"
      min_lines: 60
    - path: "components/ui/empty-state.tsx"
      provides: "Empty state with icon, message, CTA"
      min_lines: 40
    - path: "components/ui/skeleton.tsx"
      provides: "Skeleton loader with shimmer animation"
      min_lines: 30
  key_links:
    - from: "components/ui/button.tsx"
      to: "lib/design-tokens.ts"
      via: "Tailwind classes using design tokens"
      pattern: "text-primary-600|bg-primary-600"
    - from: "components/ui/badge.tsx"
      to: "lib/design-tokens.ts"
      via: "Semantic color classes"
      pattern: "bg-success-100|text-success-700"
    - from: "components/ui/stat-card.tsx"
      to: "lucide-react"
      via: "Icon imports for trends"
      pattern: "import.*TrendingUp.*from.*lucide-react"
---

<objective>
Create a professional, reusable component library using design tokens from Phase 9 Plan 01, providing enhanced buttons, cards, forms, and feedback components for the Carreira AI Hub.

Purpose: Establish consistent, high-quality UI components that align with the design system and can be used across all dashboard pages. This plan transforms basic shadcn/ui components into professional SaaS-quality components with proper semantic colors, sizes, and states.

Output: 8 enhanced/new UI components using design tokens, ready for dashboard integration.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-professional-ui-ux-enhancement/09-01-SUMMARY.md
@.planning/phases/09-professional-ui-ux-enhancement/DESIGN-SPEC.md
@.planning/phases/09-professional-ui-ux-enhancement/DESIGN-PATTERNS.md
@lib/design-tokens.ts
@components/ui/button.tsx
@components/ui/badge.tsx
@components/ui/input.tsx
@components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhanced Button Component</name>
  <files>components/ui/button.tsx</files>
  <action>
Enhance the existing Button component with professional variants, sizes, and states using design tokens:

**Variants to implement:**
- `primary`: bg-primary-600, text-white, hover:bg-primary-700
- `secondary`: bg-white, border-gray-300, text-gray-700, hover:bg-gray-50
- `outline`: bg-transparent, border-primary-600, text-primary-600, hover:bg-primary-50
- `ghost`: bg-transparent, text-gray-600, hover:bg-gray-100
- `destructive`: bg-error-600, text-white, hover:bg-error-700

**Sizes to implement:**
- `xs`: h-7 (28px), px-2, text-xs
- `sm`: h-8 (32px), px-3, text-sm
- `md`: h-10 (40px), px-4, text-base (default)
- `lg`: h-12 (48px), px-6, text-lg
- `xl`: h-14 (56px), px-8, text-lg

**States:**
- Default, hover (darker bg), active (even darker), disabled (opacity-50), loading (spinner + text)

**Implementation details:**
- Replace hardcoded colors with design token Tailwind classes (text-primary-600, bg-primary-600, etc.)
- Keep existing icon support (leftIcon, rightIcon)
- Keep existing loading state with spinner
- Add proper TypeScript types for all variants and sizes
- Add transition classes: transition-colors duration-200
- Use lucide-react Loader2 icon for loading spinner (NOT inline SVG)
- Focus ring: focus:ring-2 focus:ring-primary-500 focus:ring-offset-2

**DON'T:**
- Don't use inline color values like #0F52BA
- Don't break existing Button usage in the codebase
- Don't remove the loading spinner functionality

**Example usage after enhancement:**
```tsx
<Button variant="primary" size="md">Create Invoice</Button>
<Button variant="secondary" size="sm">Cancel</Button>
<Button variant="destructive" size="lg" isLoading>Deleting...</Button>
<Button variant="outline" leftIcon={<Plus />}>Add Customer</Button>
```
  </action>
  <verify>
Run TypeScript compilation:
```bash
npx tsc --noEmit
```
Should pass with 0 errors.

Visual check: Start dev server, create a test page with all button variants and sizes to verify visual appearance.
  </verify>
  <done>
- Button.tsx exports ButtonVariant type with 5 variants
- Button.tsx exports ButtonSize type with 5 sizes
- All button variants use design token Tailwind classes
- Loading state shows Loader2 icon from lucide-react
- TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced Badge and StatCard Components</name>
  <files>components/ui/badge.tsx, components/ui/stat-card.tsx</files>
  <action>
**A. Enhance Badge component** (components/ui/badge.tsx):

Update badge variants to use design token semantic colors:

**Variants:**
- `success` (paid): bg-success-100 (#D1FAE5), text-success-700 (#065F46)
- `warning` (pending): bg-warning-100 (#FEF3C7), text-warning-700 (#B45309)
- `error` (overdue): bg-error-100 (#FEE2E2), text-error-700 (#991B1B)
- `info`: bg-info-100, text-info-700
- `default`: bg-gray-100, text-gray-700

**Styling:**
- Padding: px-3 py-1 (12px horizontal, 4px vertical)
- Font: text-xs (12px), font-semibold
- Border-radius: rounded-full (pill shape)
- Optional dot indicator: Add optional `dot` prop to show colored circle before text

**Example usage:**
```tsx
<Badge variant="success">Paid</Badge>
<Badge variant="warning">Pending</Badge>
<Badge variant="error">Overdue</Badge>
<Badge variant="info" dot>Processing</Badge>
```

---

**B. Create NEW StatCard component** (components/ui/stat-card.tsx):

Create a professional KPI card for dashboard metrics display:

**Props interface:**
```tsx
interface StatCardProps {
  label: string;           // "Total Revenue"
  value: string;           // "$125,430.00"
  change?: string;         // "+12.5%"
  trend?: "up" | "down" | "neutral";  // Trend direction
  icon?: React.ReactNode;  // Optional icon
  description?: string;    // "vs last month"
  className?: string;
}
```

**Visual structure:**
```
┌─────────────────────────────┐
│ Label (text-xs text-gray-700)│
│ Value (text-4xl font-bold)   │
│ [Icon] Change (description)  │
└─────────────────────────────┘
```

**Styling:**
- Background: white
- Border: border border-gray-200
- Padding: p-6 (24px)
- Gap: space-y-2 (8px vertical)
- Border-radius: rounded-lg
- Shadow: hover:shadow-md transition-shadow

**Typography:**
- Label: text-xs text-gray-500 font-medium uppercase tracking-wide
- Value: text-4xl font-bold text-gray-900 tabular-nums
- Change: text-sm text-gray-700 with icon

**Icons:**
- Trend up: TrendingUp from lucide-react, text-success-600
- Trend down: TrendingDown from lucide-react, text-error-600
- Trend neutral: Minus from lucide-react, text-gray-500

**Example usage:**
```tsx
<StatCard 
  label="Total Revenue" 
  value="$125,430.00" 
  change="+12.5%" 
  trend="up"
  description="vs last month"
/>
```

**DON'T:**
- Don't hardcode colors - use design token classes
- Don't use inline styles
  </action>
  <verify>
TypeScript compilation:
```bash
npx tsc --noEmit
```

Check files exist:
```bash
ls -lh components/ui/badge.tsx components/ui/stat-card.tsx
```

Visual verification: Import and render StatCard with sample data in a test page.
  </verify>
  <done>
- Badge component uses semantic color classes from design tokens
- Badge supports 5 variants with proper colors
- Badge has optional dot indicator
- StatCard component created with all required props
- StatCard uses lucide-react icons for trends
- StatCard uses tabular-nums class for value
- Both components TypeScript types are complete
  </done>
</task>

<task type="auto">
  <name>Task 3: CurrencyInput, EmptyState, and Skeleton Components</name>
  <files>components/ui/currency-input.tsx, components/ui/empty-state.tsx, components/ui/skeleton.tsx, components/ui/input.tsx</files>
  <action>
**A. Enhance Input component** (components/ui/input.tsx):

Enhance existing input with validation states:

**Add validation state prop:**
- `state`: "default" | "error" | "success"
- Error state: border-error-500, focus:ring-error-500
- Success state: border-success-500, focus:ring-success-500
- Default state: border-gray-300, focus:ring-primary-500

**Add helper text:**
- `helperText` prop for description below input
- `errorText` prop for error messages (red text)

**Example:**
```tsx
<Input 
  label="Email" 
  type="email" 
  state="error" 
  errorText="Invalid email format"
/>
```

---

**B. Create NEW CurrencyInput component** (components/ui/currency-input.tsx):

Create a specialized input for currency values with formatting:

**Props interface:**
```tsx
interface CurrencyInputProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'onChange'> {
  label?: string;
  value: string;
  onChange: (value: string) => void;
  error?: string;
  helperText?: string;
}
```

**Features:**
- Prefix: "$" symbol (gray, not editable)
- Format: Add commas to thousands (1,234.56)
- Allow only numbers and one decimal point
- tabular-nums class for alignment
- Based on enhanced Input component

**Implementation:**
- Use Input component as base
- Wrap in div with relative positioning
- Position "$" as absolute left-3 text-gray-500
- Input padding-left adjusted: pl-7
- Format value with: `value.replace(/\B(?=(\d{3})+(?!\d))/g, ',')`
- Parse on change: Remove commas, validate number

**Example usage:**
```tsx
<CurrencyInput 
  label="Invoice Amount" 
  value={amount}
  onChange={setAmount}
  helperText="Enter total invoice amount"
/>
```

---

**C. Create NEW EmptyState component** (components/ui/empty-state.tsx):

Create a friendly empty state component:

**Props interface:**
```tsx
interface EmptyStateProps {
  icon?: React.ReactNode;
  title: string;
  description?: string;
  action?: {
    label: string;
    onClick: () => void;
  };
  className?: string;
}
```

**Visual structure:**
```
┌─────────────────────────────┐
│        [Large Icon]          │
│      Title (bold)            │
│      Description (gray)      │
│      [Action Button]         │
└─────────────────────────────┘
```

**Styling:**
- Container: text-center py-12
- Icon: w-16 h-16 mx-auto text-gray-400
- Title: text-lg font-semibold text-gray-900 mt-4
- Description: text-sm text-gray-500 mt-2
- Button: Button component with primary variant

**Default icons (lucide-react):**
- FileText for documents
- Users for customers
- DollarSign for financial data

**Example:**
```tsx
<EmptyState 
  icon={<FileText />}
  title="No invoices yet"
  description="Create your first invoice to get started"
  action={{ label: "Create Invoice", onClick: () => router.push('/create') }}
/>
```

---

**D. Create NEW Skeleton component** (components/ui/skeleton.tsx):

Create skeleton loader for loading states:

**Component:**
```tsx
interface SkeletonProps {
  className?: string;
  variant?: "text" | "rectangle" | "circle";
}

export function Skeleton({ className, variant = "rectangle" }: SkeletonProps) {
  const baseClasses = "animate-pulse bg-gray-200";
  
  const variantClasses = {
    text: "h-4 rounded",
    rectangle: "rounded-lg",
    circle: "rounded-full"
  };
  
  return <div className={cn(baseClasses, variantClasses[variant], className)} />;
}
```

**Shimmer animation (add to globals.css if needed):**
```css
@keyframes shimmer {
  0% { background-position: -1000px 0; }
  100% { background-position: 1000px 0; }
}

.animate-shimmer {
  animation: shimmer 2s infinite linear;
  background: linear-gradient(90deg, #f3f4f6 0%, #e5e7eb 50%, #f3f4f6 100%);
  background-size: 1000px 100%;
}
```

**Example usage:**
```tsx
<Skeleton className="h-32 w-full" />
<Skeleton variant="circle" className="h-12 w-12" />
<Skeleton variant="text" className="w-3/4" />
```

**DON'T:**
- Don't use complex animation libraries - Tailwind's animate-pulse is sufficient
- Don't make skeleton too bright - subtle is better
  </action>
  <verify>
TypeScript compilation:
```bash
npx tsc --noEmit
```

Check all files exist:
```bash
ls -lh components/ui/input.tsx components/ui/currency-input.tsx components/ui/empty-state.tsx components/ui/skeleton.tsx
```

Test CurrencyInput formatting:
- Input "1234.56" → should display "$1,234.56"
- Input "1000000" → should display "$1,000,000"
  </verify>
  <done>
- Input component has validation states (default, error, success)
- Input has helperText and errorText props
- CurrencyInput component created with $ prefix
- CurrencyInput formats numbers with commas
- CurrencyInput only allows numeric input
- EmptyState component created with icon, title, description, action
- Skeleton component created with 3 variants (text, rectangle, circle)
- All components use design token classes
- TypeScript types complete for all components
  </done>
</task>

</tasks>

<verification>
## Overall Phase Verification

After all tasks complete, verify the component library:

### 1. TypeScript Build Check
```bash
npx tsc --noEmit
```
Should pass with 0 errors.

### 2. Component Exports Check
```bash
grep -h "export.*function\|export.*const" components/ui/{button,badge,stat-card,currency-input,empty-state,skeleton}.tsx
```
Should show all component exports.

### 3. Design Token Usage Check
```bash
grep -r "text-primary-600\|bg-primary-600\|text-success-700\|bg-success-100" components/ui/button.tsx components/ui/badge.tsx components/ui/stat-card.tsx
```
Should find design token classes in components.

### 4. Visual Verification
Create a test page (app/test-components/page.tsx) with all components:
- All button variants and sizes
- All badge variants
- StatCard with different trends
- CurrencyInput with test values
- EmptyState
- Skeleton loaders

Start dev server and verify visual appearance at http://localhost:3000/test-components

### 5. Component Count Verification
```bash
ls -1 components/ui/{button,badge,stat-card,currency-input,empty-state,skeleton,input,card}.tsx | wc -l
```
Should return 8 (all components present).
</verification>

<success_criteria>
**Business Success:**
- Finance team has professional UI components for dashboard pages
- Component library is reusable across all pages
- Design system is consistently applied

**Technical Success:**
- All 8 components created/enhanced
- All components use design tokens (no hardcoded colors)
- TypeScript compilation passes
- Components are properly typed
- Components follow design patterns from DESIGN-PATTERNS.md

**Quality Success:**
- Button: 5 variants × 5 sizes = 25 combinations working
- Badge: 5 variants with semantic colors
- StatCard: Displays KPIs with trend indicators
- CurrencyInput: Formats currency correctly
- EmptyState: Guides users with helpful CTAs
- Skeleton: Provides smooth loading experience
- All components accessible (keyboard navigation, ARIA labels)
</success_criteria>

<output>
After completion, create `.planning/phases/09-professional-ui-ux-enhancement/09-02-SUMMARY.md` with:
- Components created/enhanced (8 total)
- Design token usage examples
- Component usage examples
- Visual screenshots (optional)
- Next steps for Phase 9 Plan 03 (Dashboard Page Redesign)
</output>
