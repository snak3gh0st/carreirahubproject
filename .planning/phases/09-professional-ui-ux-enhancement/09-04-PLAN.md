---
phase: 09-professional-ui-ux-enhancement
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - "app/dashboard/invoices/page.tsx"
  - "app/dashboard/invoices/[id]/page.tsx"
  - "app/dashboard/customers/page.tsx"
  - "app/dashboard/customers/[id]/page.tsx"
  - "app/dashboard/payments/page.tsx"
  - "app/dashboard/contracts/page.tsx"
autonomous: true

must_haves:
  truths:
    - "All data pages use professional data tables with gray headers (#FAFAFA)"
    - "Status badges use semantic colors (green=paid, amber=pending, red=overdue)"
    - "Filter buttons use design system colors and spacing"
    - "Invoices list matches Pencil mockup at x:1500, y:0"
    - "Invoice detail matches Pencil mockup at x:3000, y:0 with 2-column layout"
    - "All pages use design tokens (no hardcoded colors)"
    - "EmptyState component used for no-data scenarios"
    - "Loading states use Skeleton component"
  artifacts:
    - path: "app/dashboard/invoices/page.tsx"
      provides: "Professional invoices list with filters and data table"
      min_lines: 400
      exports: ["default InvoicesPage"]
    - path: "app/dashboard/invoices/[id]/page.tsx"
      provides: "Invoice detail with 2-column layout"
      min_lines: 300
      exports: ["default InvoiceDetailPage"]
    - path: "app/dashboard/customers/page.tsx"
      provides: "Professional customers list"
      min_lines: 300
      exports: ["default CustomersPage"]
  key_links:
    - from: "app/dashboard/invoices/page.tsx"
      to: "components/ui/badge.tsx"
      via: "Status badges in table"
      pattern: "import.*Badge.*from.*@/components/ui/badge"
    - from: "app/dashboard/invoices/[id]/page.tsx"
      to: "components/ui/stat-card.tsx"
      via: "Summary cards in sidebar"
      pattern: "import.*StatCard.*from.*@/components/ui/stat-card"
---

<objective>
Apply professional UI/UX design to all data pages (Invoices, Customers, Payments, Contracts).

**Purpose:** Transform data-heavy pages to match the Pencil mockup designs with professional tables, filters, and layouts.

**Output:** 6 redesigned pages with:
- Professional data tables with styled headers
- Semantic status badges
- Filter buttons and pills
- 2-column detail layouts with sidebars
- Breadcrumb navigation
- Responsive design for mobile/tablet/desktop
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 9 Context
@.planning/phases/09-professional-ui-ux-enhancement/DESIGN-SPEC.md
@.planning/phases/09-professional-ui-ux-enhancement/DESIGN-PATTERNS.md
@.planning/phases/09-professional-ui-ux-enhancement/SCREENS-COMPLETE.md
@.planning/phases/09-professional-ui-ux-enhancement/09-01-SUMMARY.md
@.planning/phases/09-professional-ui-ux-enhancement/09-02-SUMMARY.md
@.planning/phases/09-professional-ui-ux-enhancement/09-03-SUMMARY.md

# Current Implementations
@app/dashboard/invoices/page.tsx
@app/dashboard/invoices/[id]/page.tsx
@app/dashboard/customers/page.tsx
@app/dashboard/customers/[id]/page.tsx
@app/dashboard/payments/page.tsx
@app/dashboard/contracts/page.tsx

# Design System
@components/ui/button.tsx
@components/ui/badge.tsx
@components/ui/stat-card.tsx
@components/ui/empty-state.tsx
@components/ui/skeleton.tsx
@lib/design-tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Invoices List Page</name>
  <files>app/dashboard/invoices/page.tsx</files>
  <action>
Transform the invoices list page to match the Pencil mockup (x:1500, y:0).

**Current State Analysis:**
The page currently has:
- Complex filter system with status tabs, search, source filter, advanced filters
- Data table with 5 columns (Number, Customer, Amount, Due Date, Source, Status, Actions)
- KPI cards showing stats (Total, Paid, Pending, Overdue)
- Status distribution bar chart
- Pagination
- Mobile-responsive design
- QuickBooks integration indicators

**Changes Needed:**

1. **Page Header (Simplified):**
   Replace lines 222-232 with:
   ```tsx
   <div className="mb-8">
     <div className="flex items-center justify-between">
       <h1 className="text-3xl font-display font-semibold text-gray-900">
         Invoices
       </h1>
       <Link
         href="/dashboard/invoices/new"
         className="inline-flex items-center gap-2 px-5 py-2.5 bg-primary-600 text-white text-sm font-display font-semibold rounded-lg hover:bg-primary-700 transition-colors"
       >
         <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
           <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" />
         </svg>
         Create Invoice
       </Link>
     </div>
   </div>
   ```

2. **KPI Cards Row (Keep but Simplify):**
   Keep the existing stats cards (lines 298-372) but update styling to match StatCard design:
   - Use `bg-white` with `border border-gray-200` (hairline border #E8E8E8)
   - Keep existing structure and data
   - Update text colors: use `text-gray-700` for labels, `text-gray-900` for values
   - Keep trend indicators with semantic colors

3. **Filter Section (Streamline):**
   Simplify filters section (lines 375-566):
   - Keep search bar, status tabs, source filter
   - Remove status distribution bar chart (lines 234-295) - redundant with KPI cards
   - Keep advanced filters collapse section
   - Update filter button styles:
     ```tsx
     className="px-4 py-2 bg-white border border-gray-200 text-gray-700 text-sm font-display font-medium rounded-lg hover:bg-gray-50 transition-colors"
     ```
   - Active filter button:
     ```tsx
     className="px-4 py-2 bg-primary-600 text-white text-sm font-display font-semibold rounded-lg"
     ```

4. **Data Table (Professional Styling):**
   Update table (lines 696-871):
   
   **Table Header:**
   ```tsx
   <thead className="bg-gray-50">
     <tr>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Invoice #
       </th>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Customer
       </th>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Amount
       </th>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Status
       </th>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Date
       </th>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Actions
       </th>
     </tr>
   </thead>
   ```

   **Table Rows:**
   ```tsx
   <tbody className="bg-white divide-y divide-gray-200">
     {invoices.map((invoice) => (
       <tr key={invoice.id} className="hover:bg-gray-50 transition-colors">
         <td className="px-6 py-4 whitespace-nowrap">
           <Link href={`/dashboard/invoices/${invoice.id}`} className="text-sm font-display font-medium text-primary-600 hover:text-primary-700">
             {invoice.invoiceNumber}
           </Link>
         </td>
         <td className="px-6 py-4 whitespace-nowrap">
           <div className="text-sm font-display text-gray-900">{invoice.customer.name}</div>
           <div className="text-xs text-gray-500">{invoice.customer.email}</div>
         </td>
         <td className="px-6 py-4 whitespace-nowrap text-sm font-display font-semibold text-gray-900 tabular-nums">
           ${Number(invoice.amount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
         </td>
         <td className="px-6 py-4 whitespace-nowrap">
           <Badge variant={getStatusVariant(invoice.status)}>
             {invoice.status}
           </Badge>
         </td>
         <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-700 tabular-nums">
           {format(new Date(invoice.dueDate), 'MMM dd, yyyy')}
         </td>
         <td className="px-6 py-4 whitespace-nowrap text-sm">
           <Link href={`/dashboard/invoices/${invoice.id}`} className="text-primary-600 hover:text-primary-700 font-medium">
             View
           </Link>
         </td>
       </tr>
     ))}
   </tbody>
   ```

5. **Status Badge Mapping:**
   Add helper function at top of component:
   ```tsx
   function getStatusVariant(status: InvoiceStatus): "success" | "warning" | "error" | "info" | "default" {
     switch (status) {
       case "PAID": return "success";
       case "SENT": return "info";
       case "OVERDUE": return "error";
       case "PARTIALLY_PAID": return "warning";
       default: return "default";
     }
   }
   ```

6. **Empty State:**
   Update empty state (line 747):
   ```tsx
   {invoices.length === 0 ? (
     <tr>
       <td colSpan={6} className="px-6 py-12">
         <EmptyState
           icon="file-text"
           title="No invoices found"
           description="Try adjusting your filters or create a new invoice to get started."
           action={{
             label: "Create Invoice",
             href: "/dashboard/invoices/new"
           }}
         />
       </td>
     </tr>
   ) : (
   ```

7. **Add date-fns import:**
   ```tsx
   import { format } from 'date-fns';
   ```

8. **Remove or simplify:**
   - Status distribution bar chart (lines 234-295)
   - Quick filter chips section (lines 609-693) - keep if useful, otherwise remove
   - Consolidate mobile vs desktop filter sections

**Design Specifications:**
- Page background: `bg-gray-50` (soft gray #F9FAFB)
- Card background: `bg-white`
- Borders: `border-gray-200` (#E8E8E8)
- Table header: `bg-gray-50` (#FAFAFA)
- Typography: Space Grotesk (headings/UI), Inter (body text)
- Primary action: `bg-primary-600` (#0F52BA)
- Status colors: Use Badge component variants

**Preserve:**
- All existing logic (filters, pagination, permissions, QuickBooks sync)
- Mobile responsiveness
- Sorting functionality
- Search functionality
- Statistics calculations
  </action>
</task>

<task type="auto">
  <name>Task 2: Redesign Invoice Detail Page</name>
  <files>app/dashboard/invoices/[id]/page.tsx</files>
  <action>
Transform invoice detail page to match Pencil mockup (x:3000, y:0) with 2-column layout.

**Current State:**
The page currently has a single-column layout with tabs (details, payments, activity).

**Changes Needed:**

1. **Add Breadcrumb Navigation:**
   At the top of the content (before page header):
   ```tsx
   <nav className="mb-6" aria-label="Breadcrumb">
     <ol className="flex items-center gap-2 text-sm">
       <li>
         <Link href="/dashboard/invoices" className="text-gray-500 hover:text-gray-700 font-display">
           Invoices
         </Link>
       </li>
       <li className="text-gray-400">›</li>
       <li className="text-gray-900 font-display font-medium">
         {invoice.invoiceNumber || `INV-${invoice.id.slice(0, 8)}`}
       </li>
     </ol>
   </nav>
   ```

2. **Page Header with Status and Actions:**
   ```tsx
   <div className="mb-8">
     <div className="flex items-start justify-between mb-4">
       <div>
         <h1 className="text-3xl font-display font-semibold text-gray-900 mb-2">
           Invoice #{invoice.invoiceNumber || invoice.id.slice(0, 8)}
         </h1>
         <Badge variant={getStatusVariant(invoice.status)} size="lg">
           {invoice.status}
         </Badge>
       </div>
       <div className="flex items-center gap-3">
         {invoice.status !== "VOID" && (
           <Button variant="outline" size="md">
             <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
               <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
             </svg>
             Download PDF
           </Button>
         )}
         {canEdit && (
           <Button variant="primary" size="md" href={`/dashboard/invoices/${invoice.id}/edit`}>
             Edit Invoice
           </Button>
         )}
       </div>
     </div>
   </div>
   ```

3. **2-Column Layout:**
   Replace single-column layout with:
   ```tsx
   <div className="grid grid-cols-1 lg:grid-cols-[1fr_320px] gap-8">
     {/* Left Column - Main Content */}
     <div className="space-y-6">
       {/* Invoice Details Card */}
       <div className="bg-white border border-gray-200 rounded-lg p-6">
         <h2 className="text-xl font-display font-semibold text-gray-900 mb-6">
           Invoice Details
         </h2>
         <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
           <div>
             <label className="block text-xs font-display font-medium text-gray-500 uppercase tracking-wide mb-1">
               Customer
             </label>
             <Link 
               href={`/dashboard/customers/${invoice.customer.id}`}
               className="text-sm font-display text-primary-600 hover:text-primary-700"
             >
               {invoice.customer.name}
             </Link>
             <div className="text-xs text-gray-500 mt-1">{invoice.customer.email}</div>
           </div>
           
           <div>
             <label className="block text-xs font-display font-medium text-gray-500 uppercase tracking-wide mb-1">
               Invoice Date
             </label>
             <p className="text-sm font-display text-gray-900">
               {format(new Date(invoice.invoiceDate), 'MMMM dd, yyyy')}
             </p>
           </div>
           
           <div>
             <label className="block text-xs font-display font-medium text-gray-500 uppercase tracking-wide mb-1">
               Amount
             </label>
             <p className="text-2xl font-display font-bold text-gray-900 tabular-nums">
               ${Number(invoice.amount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
             </p>
           </div>
           
           <div>
             <label className="block text-xs font-display font-medium text-gray-500 uppercase tracking-wide mb-1">
               Due Date
             </label>
             <p className="text-sm font-display text-gray-900">
               {format(new Date(invoice.dueDate), 'MMMM dd, yyyy')}
             </p>
           </div>
         </div>
       </div>

       {/* Line Items Card (if available) */}
       {invoice.items && invoice.items.length > 0 && (
         <div className="bg-white border border-gray-200 rounded-lg p-6">
           <h2 className="text-xl font-display font-semibold text-gray-900 mb-6">
             Line Items
           </h2>
           <div className="overflow-x-auto">
             <table className="min-w-full">
               <thead className="bg-gray-50">
                 <tr>
                   <th className="px-4 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase">Description</th>
                   <th className="px-4 py-3 text-right text-xs font-display font-medium text-gray-700 uppercase">Qty</th>
                   <th className="px-4 py-3 text-right text-xs font-display font-medium text-gray-700 uppercase">Price</th>
                   <th className="px-4 py-3 text-right text-xs font-display font-medium text-gray-700 uppercase">Total</th>
                 </tr>
               </thead>
               <tbody className="divide-y divide-gray-200">
                 {invoice.items.map((item, idx) => (
                   <tr key={idx}>
                     <td className="px-4 py-3 text-sm text-gray-900">{item.description}</td>
                     <td className="px-4 py-3 text-sm text-gray-900 text-right tabular-nums">{item.quantity}</td>
                     <td className="px-4 py-3 text-sm text-gray-900 text-right tabular-nums">${item.unitPrice}</td>
                     <td className="px-4 py-3 text-sm font-semibold text-gray-900 text-right tabular-nums">${item.total}</td>
                   </tr>
                 ))}
               </tbody>
             </table>
           </div>
         </div>
       )}
     </div>

     {/* Right Column - Sidebar */}
     <div className="space-y-6">
       {/* Summary Card */}
       <div className="bg-white border border-gray-200 rounded-lg p-6">
         <h3 className="text-lg font-display font-semibold text-gray-900 mb-4">
           Summary
         </h3>
         <div className="space-y-3">
           <div className="flex justify-between text-sm">
             <span className="text-gray-600">Subtotal</span>
             <span className="font-display font-medium text-gray-900 tabular-nums">
               ${Number(invoice.amount - (invoice.taxAmount || 0)).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
             </span>
           </div>
           {invoice.taxAmount && invoice.taxAmount > 0 && (
             <div className="flex justify-between text-sm">
               <span className="text-gray-600">Tax</span>
               <span className="font-display font-medium text-gray-900 tabular-nums">
                 ${Number(invoice.taxAmount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
               </span>
             </div>
           )}
           <div className="border-t border-gray-200 pt-3 flex justify-between">
             <span className="text-base font-display font-semibold text-gray-900">Total</span>
             <span className="text-xl font-display font-bold text-gray-900 tabular-nums">
               ${Number(invoice.amount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
             </span>
           </div>
         </div>
       </div>

       {/* Payment Info Card */}
       {invoice.paymentDate && (
         <div className="bg-white border border-gray-200 rounded-lg p-6">
           <h3 className="text-lg font-display font-semibold text-gray-900 mb-4">
             Payment Info
           </h3>
           <div className="space-y-3">
             <div>
               <label className="block text-xs font-display font-medium text-gray-500 uppercase mb-1">
                 Payment Method
               </label>
               <p className="text-sm text-gray-900">{invoice.paymentMethod || "Not specified"}</p>
             </div>
             <div>
               <label className="block text-xs font-display font-medium text-gray-500 uppercase mb-1">
                 Paid On
               </label>
               <p className="text-sm text-gray-900">
                 {format(new Date(invoice.paymentDate), 'MMMM dd, yyyy')}
               </p>
             </div>
           </div>
         </div>
       )}

       {/* Contract Card (if available) */}
       {invoice.deal?.contract && (
         <div className="bg-white border border-gray-200 rounded-lg p-6">
           <h3 className="text-lg font-display font-semibold text-gray-900 mb-4">
             Contract
           </h3>
           <div className="space-y-3">
             <div className="flex items-center gap-2">
               <Badge variant="success">Signed</Badge>
               <span className="text-xs text-gray-500">
                 {format(new Date(invoice.deal.contract.signedAt), 'MMM dd, yyyy')}
               </span>
             </div>
             <Button variant="ghost" size="sm" className="w-full justify-center">
               <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
               </svg>
               View Contract
             </Button>
           </div>
         </div>
       )}

       {/* QuickBooks Sync Info */}
       {invoice.quickbooks_invoice_id && (
         <div className="bg-green-50 border border-green-200 rounded-lg p-4">
           <div className="flex items-start gap-3">
             <svg className="w-5 h-5 text-green-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
               <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
             </svg>
             <div>
               <p className="text-sm font-display font-medium text-green-900">
                 Synced with QuickBooks
               </p>
               <p className="text-xs text-green-700 mt-1">
                 ID: {invoice.quickbooks_invoice_id}
               </p>
             </div>
           </div>
         </div>
       )}
     </div>
   </div>
   ```

4. **Add Required Imports:**
   ```tsx
   import { format } from 'date-fns';
   import { Badge } from '@/components/ui/badge';
   import { Button } from '@/components/ui/button';
   ```

5. **Helper Function:**
   ```tsx
   function getStatusVariant(status: InvoiceStatus): "success" | "warning" | "error" | "info" | "default" {
     switch (status) {
       case "PAID": return "success";
       case "SENT": return "info";
       case "OVERDUE": return "error";
       case "PARTIALLY_PAID": return "warning";
       default: return "default";
     }
   }
   ```

**Design Specifications:**
- 2-column grid: `grid-cols-[1fr_320px]` (main content fills, sidebar fixed 320px)
- Card spacing: 24px gap (`gap-6`)
- Label style: 10px uppercase with tracking
- Amount display: Large, bold, tabular numbers
- All borders: `border-gray-200` (#E8E8E8)
- Background: `bg-gray-50` page, `bg-white` cards

**Preserve:**
- All existing data fetching logic
- Permissions checks
- Tabs functionality (can be moved to separate cards in left column)
- QuickBooks integration indicators
  </action>
</task>

<task type="auto">
  <name>Task 3: Redesign Customers List Page</name>
  <files>app/dashboard/customers/page.tsx</files>
  <action>
Apply same professional design pattern as Invoices List to Customers page.

**Pattern to Follow:**
Same structure as Task 1 (Invoices List), adapted for customers data.

**Changes:**

1. **Page Header:**
   ```tsx
   <div className="mb-8">
     <div className="flex items-center justify-between">
       <h1 className="text-3xl font-display font-semibold text-gray-900">
         Customers
       </h1>
       <Link
         href="/dashboard/customers/new"
         className="inline-flex items-center gap-2 px-5 py-2.5 bg-primary-600 text-white text-sm font-display font-semibold rounded-lg hover:bg-primary-700 transition-colors"
       >
         <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
           <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" />
         </svg>
         Add Customer
       </Link>
     </div>
   </div>
   ```

2. **KPI Cards:**
   Keep existing stats but update card styling to match design system:
   - Total Customers
   - Active Customers
   - Total Revenue
   - Average Customer Value

3. **Data Table:**
   Update table with professional styling:
   
   **Columns:**
   - Name (with email below in gray)
   - Total Invoiced
   - Balance
   - Status Badge
   - Actions

   **Table Styling:**
   ```tsx
   <thead className="bg-gray-50">
     <tr>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Name
       </th>
       <th className="px-6 py-3 text-right text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Total Invoiced
       </th>
       <th className="px-6 py-3 text-right text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Balance
       </th>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Status
       </th>
       <th className="px-6 py-3 text-left text-xs font-display font-medium text-gray-700 uppercase tracking-wide">
         Actions
       </th>
     </tr>
   </thead>
   ```

4. **Status Badge Logic:**
   ```tsx
   function getCustomerStatus(customer: any): { variant: BadgeVariant; label: string } {
     const hasOverdue = customer.invoices.some((inv: any) => inv.status === "OVERDUE");
     const hasActive = customer.invoices.some((inv: any) => inv.status !== "PAID" && inv.status !== "VOID");
     
     if (hasOverdue) return { variant: "error", label: "Overdue" };
     if (hasActive) return { variant: "info", label: "Active" };
     return { variant: "success", label: "Good Standing" };
   }
   ```

5. **Empty State:**
   ```tsx
   <EmptyState
     icon="users"
     title="No customers found"
     description="Get started by adding your first customer."
     action={{
       label: "Add Customer",
       href: "/dashboard/customers/new"
     }}
   />
   ```

**Apply same design tokens:**
- `bg-gray-50` page background
- `bg-white` cards with `border-gray-200`
- `text-gray-700` for labels, `text-gray-900` for values
- `bg-gray-50` table headers
- Space Grotesk for headings, Inter for body text
- Tabular numbers for financial data

**Preserve:**
- All filtering logic
- Search functionality
- Pagination
- Permissions
- QuickBooks sync indicators
  </action>
</task>

<task type="auto">
  <name>Task 4: Redesign Customer Detail Page</name>
  <files>app/dashboard/customers/[id]/page.tsx</files>
  <action>
Apply same 2-column layout pattern as Invoice Detail to Customer Detail page.

**Layout Structure:**

1. **Breadcrumb:**
   ```tsx
   <nav className="mb-6" aria-label="Breadcrumb">
     <ol className="flex items-center gap-2 text-sm">
       <li>
         <Link href="/dashboard/customers" className="text-gray-500 hover:text-gray-700 font-display">
           Customers
         </Link>
       </li>
       <li className="text-gray-400">›</li>
       <li className="text-gray-900 font-display font-medium">
         {customer.name}
       </li>
     </ol>
   </nav>
   ```

2. **2-Column Grid:**
   ```tsx
   <div className="grid grid-cols-1 lg:grid-cols-[1fr_320px] gap-8">
     {/* Left Column */}
     <div className="space-y-6">
       {/* Contact Information Card */}
       <div className="bg-white border border-gray-200 rounded-lg p-6">
         <h2 className="text-xl font-display font-semibold text-gray-900 mb-6">
           Contact Information
         </h2>
         {/* Contact details grid */}
       </div>

       {/* Installment Summary Card */}
       <div className="bg-white border border-gray-200 rounded-lg p-6">
         <h2 className="text-xl font-display font-semibold text-gray-900 mb-6">
           Installment Summary
         </h2>
         <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
           {/* Total Invoices, Paid, Left to Pay, Overdue */}
         </div>
       </div>

       {/* Invoices List Card */}
       <div className="bg-white border border-gray-200 rounded-lg p-6">
         <h2 className="text-xl font-display font-semibold text-gray-900 mb-6">
           Invoices
         </h2>
         {/* Mini table with customer's invoices */}
       </div>
     </div>

     {/* Right Column - Sidebar */}
     <div className="space-y-6">
       {/* Financial Summary Card */}
       <div className="bg-white border border-gray-200 rounded-lg p-6">
         <h3 className="text-lg font-display font-semibold text-gray-900 mb-4">
           Financial Summary
         </h3>
         <div className="space-y-4">
           <div>
             <label className="text-xs text-gray-500 uppercase tracking-wide">Total Invoiced</label>
             <p className="text-2xl font-display font-bold text-gray-900 tabular-nums mt-1">
               ${totalInvoiced}
             </p>
           </div>
           <div>
             <label className="text-xs text-gray-500 uppercase tracking-wide">Total Paid</label>
             <p className="text-2xl font-display font-bold text-success-600 tabular-nums mt-1">
               ${totalPaid}
             </p>
           </div>
           <div>
             <label className="text-xs text-gray-500 uppercase tracking-wide">Balance</label>
             <p className="text-2xl font-display font-bold text-gray-900 tabular-nums mt-1">
               ${balance}
             </p>
           </div>
         </div>
       </div>

       {/* Payment History Timeline */}
       <div className="bg-white border border-gray-200 rounded-lg p-6">
         <h3 className="text-lg font-display font-semibold text-gray-900 mb-4">
           Recent Payments
         </h3>
         {/* Timeline or list of recent payments */}
       </div>
     </div>
   </div>
   ```

**Apply same design system:**
- 2-column layout with 320px sidebar
- Professional card styling with borders
- Tabular numbers for financial data
- Badge components for status
- Space Grotesk + Inter typography

**Preserve:**
- All data fetching
- Permissions
- QuickBooks integration
  </action>
</task>

<task type="auto">
  <name>Task 5: Redesign Payments Page</name>
  <files>app/dashboard/payments/page.tsx</files>
  <action>
Apply professional list page design to Payments page (similar to Invoices List).

**Changes:**

1. **Page Header:**
   ```tsx
   <div className="mb-8">
     <h1 className="text-3xl font-display font-semibold text-gray-900">
       Payments
     </h1>
   </div>
   ```

2. **Data Table Columns:**
   - Payment ID (or date as primary identifier)
   - Customer (name + email)
   - Invoice Number (linked)
   - Amount
   - Payment Method Badge
   - Date

3. **Table Styling:**
   Same professional styling as Invoices table:
   - `bg-gray-50` header
   - Hairline borders
   - Hover states
   - Tabular numbers for amounts

4. **Payment Method Badge:**
   ```tsx
   function getPaymentMethodVariant(method: string): BadgeVariant {
     switch (method) {
       case "CARD": return "info";
       case "BANK_TRANSFER": return "success";
       case "CASH": return "warning";
       default: return "default";
     }
   }
   ```

5. **Filters:**
   - Date range picker
   - Payment method dropdown
   - Search by customer name or invoice number

**Apply design system:**
- Professional table styling
- Badge components for payment methods
- Filter buttons with design tokens
- Empty state component

**Preserve:**
- Data fetching logic
- Filtering and sorting
- Pagination
  </action>
</task>

<task type="auto">
  <name>Task 6: Redesign Contracts Page</name>
  <files>app/dashboard/contracts/page.tsx</files>
  <action>
Apply professional list page design to Contracts page.

**Changes:**

1. **Page Header:**
   ```tsx
   <div className="mb-8">
     <div className="flex items-center justify-between">
       <h1 className="text-3xl font-display font-semibold text-gray-900">
         Contracts
       </h1>
       <Link
         href="/dashboard/contracts/new"
         className="inline-flex items-center gap-2 px-5 py-2.5 bg-primary-600 text-white text-sm font-display font-semibold rounded-lg hover:bg-primary-700 transition-colors"
       >
         <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
           <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" />
         </svg>
         New Contract
       </Link>
     </div>
   </div>
   ```

2. **Data Table Columns:**
   - Contract ID
   - Customer (name + email)
   - Status Badge
   - Sent Date
   - Signed Date (if applicable)
   - Actions (View/Download button)

3. **Status Badge Variants:**
   ```tsx
   function getContractStatus(contract: any): { variant: BadgeVariant; label: string } {
     if (contract.status === "SIGNED") return { variant: "success", label: "Signed" };
     if (contract.status === "SENT") return { variant: "info", label: "Sent" };
     if (contract.status === "EXPIRED") return { variant: "error", label: "Expired" };
     return { variant: "default", label: "Draft" };
   }
   ```

4. **Actions Column:**
   ```tsx
   <td className="px-6 py-4 whitespace-nowrap">
     <div className="flex items-center gap-3">
       <Link
         href={`/dashboard/contracts/${contract.id}`}
         className="text-primary-600 hover:text-primary-700 font-medium text-sm"
       >
         View
       </Link>
       {contract.docusign_envelope_id && contract.status === "SIGNED" && (
         <button className="text-primary-600 hover:text-primary-700 font-medium text-sm">
           Download
         </button>
       )}
     </div>
   </td>
   ```

5. **Filters:**
   - Status filter (Draft, Sent, Signed, Expired)
   - Date range (sent date, signed date)
   - Customer search

**Apply design system:**
- Professional table styling
- Badge components for contract status
- Filter buttons
- Empty state for no contracts

**Preserve:**
- DocuSign integration logic
- PDF download functionality
- Permissions
  </action>
</task>

</tasks>

<testing>
**Manual Testing Checklist:**

1. **Invoices List:**
   - [ ] Page header displays correctly with "Create Invoice" button
   - [ ] KPI cards show accurate stats
   - [ ] Data table has professional gray header
   - [ ] Status badges use correct colors (green/amber/red)
   - [ ] Filter buttons styled correctly (active vs inactive)
   - [ ] Table rows have hover effect
   - [ ] Empty state displays when no invoices
   - [ ] Pagination works
   - [ ] Mobile responsive

2. **Invoice Detail:**
   - [ ] Breadcrumb navigation works
   - [ ] 2-column layout displays correctly (desktop)
   - [ ] Sidebar is 320px fixed width
   - [ ] Invoice details card shows all info
   - [ ] Summary card calculates totals correctly
   - [ ] Payment info card displays when paid
   - [ ] Contract card appears when contract exists
   - [ ] QuickBooks sync indicator shows correctly
   - [ ] Responsive on mobile (columns stack)

3. **Customers List:**
   - [ ] Same professional styling as Invoices
   - [ ] Status badges show customer health
   - [ ] Financial data uses tabular numbers
   - [ ] Search and filters work

4. **Customer Detail:**
   - [ ] 2-column layout like Invoice Detail
   - [ ] Financial summary displays correctly
   - [ ] Invoices list shows customer's invoices
   - [ ] Payment history timeline visible

5. **Payments Page:**
   - [ ] Professional table styling
   - [ ] Payment method badges colored correctly
   - [ ] Amounts are tabular
   - [ ] Filters work

6. **Contracts Page:**
   - [ ] Status badges (Draft/Sent/Signed/Expired) correct colors
   - [ ] Download button works for signed contracts
   - [ ] Filters functional

**Visual Regression:**
- [ ] All pages use design tokens (no hardcoded colors)
- [ ] Typography consistent (Space Grotesk + Inter)
- [ ] Borders are hairline (#E8E8E8)
- [ ] Table headers are gray (#FAFAFA)
- [ ] Page background is soft gray (#F9FAFB)
- [ ] Hover states smooth transitions

**Browser Testing:**
- [ ] Chrome/Edge (latest)
- [ ] Safari (latest)
- [ ] Firefox (latest)
- [ ] Mobile Safari (iOS)
- [ ] Chrome Mobile (Android)

**Accessibility:**
- [ ] Color contrast meets WCAG 2.1 AA
- [ ] Keyboard navigation works
- [ ] Screen reader labels present
- [ ] Focus indicators visible
</testing>

<dependencies>
**Required Components (from Plan 09-02):**
- ✅ Badge component with 5 variants
- ✅ Button component with variants
- ✅ StatCard component
- ✅ EmptyState component
- ✅ Skeleton component
- ✅ Input component

**Required Libraries:**
- ✅ date-fns (already installed for date formatting)
- ✅ lucide-react (already installed for icons)

**Design System:**
- ✅ Design tokens in tailwind.config.ts
- ✅ CSS custom properties in globals.css
- ✅ TypeScript tokens in lib/design-tokens.ts
</dependencies>

<notes>
**Estimated Time:** 75-90 minutes

**Complexity:** High
- 6 pages to redesign
- Multiple data tables with different schemas
- 2-column layouts for detail pages
- Responsive design considerations
- Preserving complex filtering/sorting logic

**Risk Mitigation:**
- Keep all existing logic intact (only update UI)
- Test each page individually before moving to next
- Use existing Badge/Button components (already tested)
- Follow same pattern for all list pages (consistency)

**Performance:**
- No new database queries (use existing data fetching)
- CSS-only styling changes (no runtime overhead)
- Tailwind classes are tree-shaken (small bundle)

**Success Criteria:**
- All 6 pages match design system
- No functional regressions
- Mobile responsive
- TypeScript compiles with 0 errors
- Visual consistency across all pages
</notes>
