---
phase: 04-production-auth
plan: 01
type: execute
domain: backend
---

<objective>
Implement password hashing with bcrypt and remove development authentication bypass to enable production-ready user authentication.

Purpose: Secure user authentication by storing hashed passwords instead of bypassing password checks. Current system allows any user with valid email to log in without password verification, creating a critical security vulnerability.

Output: User model with password field, bcrypt-based password hashing for new/updated passwords, password verification in login flow, development bypass removed.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-queue-processing/03-02-SUMMARY.md

**Key Context from Phase 3:**
- Queue processing is production-ready (cron-based processing, monitoring in place)
- No blocking dependencies for auth work
- IntegrationLog is available for audit logging if needed

**Accumulated Decisions from Phases 1-3:**
- Always return 200 OK to webhook senders (prevents cascades)
- Use exponential backoff with 5 retries for external APIs
- Implement circuit breakers for external services
- Use cron-based polling for Vercel compatibility
- Structured error logging via IntegrationLog

**Current Auth Implementation:**
- lib/auth.ts: NextAuth.js with CredentialsProvider
- User model: Has email (unique), name, role, active status
- Missing: password field, password hashing, password verification
- Development bypass: Line 47-49 in lib/auth.ts skip password check

**Files to Modify:**
- prisma/schema.prisma - Add optional password field to User model
- lib/auth.ts - Implement bcrypt verification, remove bypass
- lib/services/auth.service.ts - Create new service for password operations

**Dependencies:**
- bcrypt (npm package) - for password hashing/verification
- Must be backward compatible with existing users who don't have passwords yet
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add password field to User model</name>
  <files>prisma/schema.prisma</files>
  <action>
    Add optional password field to User model:
    - Add `password String?` field (nullable, to allow gradual migration)
    - Add `passwordHashedAt DateTime?` field (tracks when password was last set, for audit)
    - Position after email field
    - Include index on id for faster lookups: @@index([id])

    After editing, run `npm run db:generate` to regenerate Prisma Client.

    Why optional: Current users don't have passwords yet. This allows them to continue working while you implement the new password flow. Later you can require passwords during next login or via admin panel.
  </action>
  <verify>npm run db:generate returns success with no errors</verify>
  <done>User model has password field (nullable), passwordHashedAt field, schema valid, Prisma types generated</done>
</task>

<task type="auto">
  <name>Task 2: Create password hashing service with bcrypt</name>
  <files>lib/services/auth.service.ts</files>
  <action>
    Create new `lib/services/auth.service.ts` file with:

    1. Import bcrypt (already in package.json or install if needed)
    2. Export class `AuthService` with these methods:

    - `async hashPassword(password: string): Promise<string>`
      - Validate password length (min 8 chars, explain why in comment)
      - Use bcrypt.hash(password, 12) for salting/hashing
      - Return hashed password
      - Throw error if password too short

    - `async verifyPassword(password: string, hash: string): Promise<boolean>`
      - Use bcrypt.compare(password, hash)
      - Return boolean result
      - Return false (never throw) on invalid input

    - Export singleton: `export const authService = new AuthService()`

    Add JSDoc comments explaining salt rounds (12 = good security/performance balance).

    Why separate service: Password operations are reusable, testable, and isolated from auth logic.
  </action>
  <verify>File created, TypeScript compiles without errors, methods exported correctly</verify>
  <done>AuthService class created with hashPassword, verifyPassword methods, singleton exported, bcrypt imported</done>
</task>

<task type="auto">
  <name>Task 3: Implement password verification in auth callback</name>
  <files>lib/auth.ts</files>
  <action>
    Update CredentialsProvider authorize callback in lib/auth.ts:

    1. Import authService at top: `import { authService } from "@/lib/services/auth.service";`

    2. In authorize function, after checking user.active:
       - Query user with password field included: select { ..., password: true }
       - If user has NO password (null/undefined):
         * Log to console: "[AUTH] User has no password set, please set password first"
         * Return null to reject login
         * This prompts user to set password before logging in (Phase 5 feature)

       - If user HAS password:
         * Call `authService.verifyPassword(credentials.password, user.password)`
         * If verification fails, return null
         * If verification succeeds, continue with existing flow

    3. Remove TODO comment and dev bypass (lines 47-50)

    Why phase it this way: Existing users without passwords can't log in, prompting password reset flow (future). This prevents unauthorized access.

    Safety: Never log actual password, never expose hashing details to frontend.
  </action>
  <verify>lib/auth.ts updated, TypeScript compiles, CredentialsProvider includes password verification</verify>
  <done>Password verification implemented, development bypass removed, users without passwords rejected at login</done>
</task>

<task type="auto">
  <name>Task 4: Create password setting endpoint for initial setup</name>
  <files>app/api/auth/set-password/route.ts</files>
  <action>
    Create new endpoint `app/api/auth/set-password/route.ts` that allows users to set their password for the first time or reset it:

    1. POST endpoint accepting: { password: string }

    2. Validate:
       - User is authenticated (get from session)
       - Password length >= 8 chars (return 400 if not)
       - Throw error for unauthenticated requests

    3. Implementation:
       - Call authService.hashPassword(password) to hash new password
       - Update user: `prisma.user.update({ where: { id: userId }, data: { password: hashed, passwordHashedAt: new Date() } })`
       - Return { success: true, message: "Password set successfully" }

    4. Error handling:
       - Log to console if update fails
       - Return 500 with generic error message (never expose internal details)

    5. Documentation comment:
       - Explain this is for initial password setup
       - Note: After setting password, user must log in again with new password
       - Future: Can be called from password-reset UI

    Why this endpoint: Users without passwords need a way to set them. NextAuth doesn't provide signup/password-change by default.
  </action>
  <verify>Endpoint created, TypeScript compiles, responds to POST with valid password</verify>
  <done>Set-password endpoint created, validates password length, hashes with bcrypt, updates user in database</done>
</task>

<task type="auto">
  <name>Task 5: Build and test password flow</name>
  <files>N/A - verification only</files>
  <action>
    Run full build and test the password flow locally:

    1. Run: `npm run build`
       - Should compile without errors
       - Should generate Prisma types correctly

    2. Test locally:
       - Start dev server: `npm run dev`
       - Create test user: `npm run user:create` (creates user with email/no password)
       - Try to log in with that email + password "testpassword" → Should fail (user has no password)
       - Call POST /api/auth/set-password with { password: "testpassword123" } → Should return 200
       - Try to log in again with email + "testpassword123" → Should succeed
       - Try to log in with wrong password → Should fail

    3. Verify no errors in console, auth flow works end-to-end

    Why separate from implementation: Verifies integration works correctly before committing.
  </action>
  <verify>npm run build succeeds, local testing shows password login/verification working correctly</verify>
  <done>Build passes, password setting endpoint works, login verification with bcrypt confirmed working</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without TypeScript errors
- [ ] `npm run db:generate` completes with User model changes
- [ ] Password field added to User model (nullable)
- [ ] authService exports hashPassword and verifyPassword methods
- [ ] lib/auth.ts includes password verification logic
- [ ] Development bypass removed from auth callback
- [ ] /api/auth/set-password endpoint created and functional
- [ ] Local testing: user without password rejected at login
- [ ] Local testing: user with password can log in successfully
- [ ] Local testing: wrong password rejected
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Password field added to User model
- bcrypt password hashing implemented
- Password verification in login flow
- Development bypass removed
- Set-password endpoint available for users without passwords
  </success_criteria>

<output>
After completion, create `.planning/phases/04-production-auth/04-01-SUMMARY.md` with:

# Phase 4 Plan 1: Password Hashing Implementation Summary

**Substantive one-liner: Implemented password hashing with bcrypt, removed development bypass, and created password setup endpoint for production-ready authentication.**

## Accomplishments

- Added password field to User model (optional for backward compatibility)
- Created AuthService with bcrypt-based password hashing and verification
- Implemented password verification in NextAuth CredentialsProvider
- Removed development authentication bypass
- Created /api/auth/set-password endpoint for initial password setup

## Files Created/Modified

### Created
- lib/services/auth.service.ts - Password hashing and verification service
- app/api/auth/set-password/route.ts - Password setup endpoint

### Modified
- prisma/schema.prisma - Added password and passwordHashedAt fields to User
- lib/auth.ts - Implemented password verification, removed dev bypass

## Decisions Made

1. **Optional Password Field**: Made password nullable to allow gradual migration
   - Rationale: Existing users without passwords won't be locked out immediately
   - Next step: Create password reset flow (Phase 4 or later)

2. **12-Round bcrypt**: Used cost factor of 12 for password hashing
   - Rationale: Balance between security (prevents brute force) and performance (~100ms hash time)

3. **Separate AuthService**: Created dedicated service for password operations
   - Rationale: Reusable, testable, isolated from auth logic

4. **Reject Users Without Passwords**: Users without passwords can't log in
   - Rationale: Forces migration to password-based auth, prevents unintended access
   - Mitigation: /api/auth/set-password available for initial setup

## Issues Encountered

None - Implementation followed plan exactly.

## Next Phase Readiness

**Ready for 04-02: QuickBooks OAuth Token Refresh**
- Password infrastructure is in place
- Users can now set and verify passwords
- No blocking dependencies for OAuth work
</output>
