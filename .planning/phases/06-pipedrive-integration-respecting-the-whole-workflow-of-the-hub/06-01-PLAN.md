---
phase: 06-pipedrive-integration-respecting-the-whole-workflow-of-the-hub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/webhooks/pipedrive/deal/route.ts
  - app/api/webhooks/pipedrive/person/route.ts
  - lib/services/identity-mapper.ts
autonomous: true

must_haves:
  truths:
    - "Pipedrive deal won webhook no longer creates invoices"
    - "Pipedrive person webhook matches customers by email with QuickBooks"
    - "Person webhook creates Lead in Hub when no QB customer exists"
    - "Person webhook links to existing Customer when QB customer found"
  artifacts:
    - path: "app/api/webhooks/pipedrive/deal/route.ts"
      provides: "Deal webhook handler with correct workflow (no invoice creation)"
      min_lines: 80
    - path: "app/api/webhooks/pipedrive/person/route.ts"
      provides: "Person webhook with QB customer matching"
      min_lines: 150
  key_links:
    - from: "app/api/webhooks/pipedrive/person/route.ts"
      to: "lib/services/identity-mapper.ts"
      via: "identityMapper.reconcileCustomer()"
      pattern: "identityMapper\\.reconcileCustomer"
    - from: "app/api/webhooks/pipedrive/person/route.ts"
      to: "prisma.customer"
      via: "Customer lookup by email"
      pattern: "prisma\\.customer\\.findUnique.*email"
---

<objective>
Fix backwards workflow and establish correct lead entry from Pipedrive to Hub.

Purpose: Prevent incorrect invoice creation from deal won events and enable proper lead/customer sync with email-based matching to QuickBooks customers.

Output: Corrected webhook handlers that respect QuickBooks as financial source of truth and properly link Pipedrive persons to existing customers.
</objective>

<execution_context>
@~/.config/Claude/get-shit-done/workflows/execute-plan.md
@~/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipedrive-integration-respecting-the-whole-workflow-of-the-hub/06-CONTEXT.md
@.planning/phases/06-pipedrive-integration-respecting-the-whole-workflow-of-the-hub/06-RESEARCH.md

# Existing infrastructure
@lib/services/identity-mapper.ts
@lib/services/pipedrive.service.ts
@app/api/webhooks/pipedrive/deal/route.ts
@app/api/webhooks/pipedrive/person/route.ts
</context>

<tasks>

<task type="auto">
  <name>Remove invoice creation from deal won webhook</name>
  <files>app/api/webhooks/pipedrive/deal/route.ts</files>
  <action>
CRITICAL FIX: The current deal webhook implements backwards workflow (deal won → creates invoice). This is INCORRECT per research findings (06-RESEARCH.md lines 47-55) and user requirements (06-CONTEXT.md line 28).

Remove all invoice/contract creation logic from this webhook:
1. Keep webhook acceptance and validation (HMAC, deduplication)
2. Keep Deal update in database (status sync from Pipedrive)
3. REMOVE calls to invoiceWorkflowService.processDealWon()
4. REMOVE any invoice or contract generation
5. Log deal status change to IntegrationLog only
6. Add comment explaining correct workflow: "Invoice creation drives deal updates, not vice versa"

Reference research: 06-RESEARCH.md "Pitfall 2: Creating Invoices from Deal Won Webhook" (lines 331-350)

The deal won webhook should ONLY:
- Update Hub deal status to match Pipedrive
- Log the event
- NOT trigger any financial workflows
  </action>
  <verify>
grep -n "invoiceWorkflowService" app/api/webhooks/pipedrive/deal/route.ts
# Should return NO matches

grep -n "processDealWon" app/api/webhooks/pipedrive/deal/route.ts
# Should return NO matches

cat app/api/webhooks/pipedrive/deal/route.ts | grep -A 5 "Deal won"
# Should show only status update, no invoice creation
  </verify>
  <done>Deal webhook no longer triggers invoice creation. Webhook only updates deal status in Hub and logs event to IntegrationLog.</done>
</task>

<task type="auto">
  <name>Enhance person webhook with QuickBooks customer matching</name>
  <files>app/api/webhooks/pipedrive/person/route.ts</files>
  <action>
Implement email-based customer matching between Pipedrive persons and QuickBooks customers per 06-RESEARCH.md Pattern 1 (lines 94-120) and user requirements (06-CONTEXT.md lines 9-13).

After webhook validation and before identity reconciliation:

1. Query existing Customer by email:
```typescript
const existingCustomer = await prisma.customer.findUnique({
  where: { email },
  select: { 
    id: true, 
    email: true, 
    quickbooks_id: true, 
    pipedrive_id: true,
    lastPipedriveSyncAt: true
  }
});
```

2. If customer exists with quickbooks_id:
   - Update pipedrive_id field
   - Update lastPipedriveSyncAt
   - Log success: "Linked Pipedrive person to existing QB customer"
   - Return 200 OK

3. If customer exists WITHOUT quickbooks_id (manual Hub entry):
   - Update pipedrive_id
   - Log: "Linked Pipedrive person to Hub customer (no QB sync)"
   - Return 200 OK

4. If NO customer exists:
   - Create Lead in database (NOT Customer - no QB customer yet)
   - Store pipedrive_id in Lead table
   - Log: "Created Lead from Pipedrive person"
   - Return 200 OK

5. Keep existing 5-second debounce logic (lines 110-128 in current file) to prevent webhook loops

Reference existing debounce pattern from 06-RESEARCH.md lines 129-152
Use Identity Mapper pattern from lib/services/identity-mapper.ts for any customer creation
  </action>
  <verify>
grep -n "quickbooks_id" app/api/webhooks/pipedrive/person/route.ts
# Should show QB customer matching logic

grep -n "prisma.lead.create" app/api/webhooks/pipedrive/person/route.ts
# Should show Lead creation for non-QB persons

grep -n "lastPipedriveSyncAt" app/api/webhooks/pipedrive/person/route.ts
# Should show debounce logic intact

npm run build
# Should compile without errors
  </verify>
  <done>Person webhook matches Pipedrive persons to existing QuickBooks customers by email. If QB customer exists, links pipedrive_id. If no QB customer, creates Lead. Debounce logic prevents webhook loops.</done>
</task>

</tasks>

<verification>
**Integration test:**
1. Check IntegrationLog table for deal webhook events - should NOT show invoice creation
2. Test person webhook with existing QB customer email - should link pipedrive_id
3. Test person webhook with new email - should create Lead (not Customer)

**Code review:**
- Deal webhook has NO references to invoiceWorkflowService or processDealWon
- Person webhook has explicit QB customer matching logic
- Debounce timestamps prevent webhook loops
</verification>

<success_criteria>
- Deal won webhook no longer triggers invoice/contract generation
- Person webhook links Pipedrive persons to existing QB customers via email match
- Person webhook creates Leads for persons without QB customers
- Webhook loop prevention (debounce) intact
- All changes logged to IntegrationLog table
- Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipedrive-integration-respecting-the-whole-workflow-of-the-hub/06-01-SUMMARY.md`
</output>
