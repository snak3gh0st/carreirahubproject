---
phase: 4.1-deployment-ready
type: execute
---

<objective>
Create comprehensive Payments dashboard page to give Finance team complete visibility into payment history, transaction details, and reconciliation data.

Purpose: Finance team cannot currently view payment data in isolation — they must navigate through invoices or customers. A dedicated payments page is essential for daily financial operations and reconciliation.

Output: `/dashboard/payments` page with list view, advanced filtering, payment detail view, and QuickBooks sync status indicators.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/4.1-user-deployment/4.1-CONTEXT.md
@app/dashboard/invoices/page.tsx
@app/dashboard/customers/page.tsx
@lib/services/quickbooks-sync.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create payments list page with filtering and pagination</name>
  <files>app/dashboard/payments/page.tsx</files>
  <action>
    Create `/dashboard/payments` page following the patterns established in invoices and customers pages:

    **Data Fetching:**
    - Query Payment table with relations: customer, invoice
    - Include pagination (25 records per page, configurable via searchParams)
    - Support advanced filtering: date range, amount range, payment method, customer
    - Support search by transaction reference number
    - Order by payment date DESC (most recent first)
    - Calculate summary metrics: total payments, average payment amount, payment count

    **Layout Structure:**
    - Summary cards at top: Total Payments (sum), Average Payment, Payment Count, This Month Total
    - Advanced filter panel (collapsible) with:
      - Date range picker (from/to)
      - Amount range (min/max)
      - Payment method dropdown (multiple select)
      - Customer search/select
      - Clear filters button
    - Quick filter chips (like invoices page):
      - Today
      - This Week
      - This Month
      - Last 30 Days
      - High Value (>$5000)
      - QuickBooks Synced
      - Manual Entry
    - Payment table with columns:
      - Date (formatted)
      - Transaction Ref # (with copy button)
      - Customer Name (link to customer detail)
      - Invoice # (link to invoice detail)
      - Amount (formatted currency)
      - Payment Method (badge)
      - Source (QuickBooks/Manual badge)
      - Status (Applied/Pending/Failed badge)

    **Table Features:**
    - Sortable columns (date, amount)
    - Click row to navigate to payment detail page
    - Hover effects for better UX
    - Mobile-responsive table (horizontal scroll on small screens)
    - Empty state if no payments
    - Loading skeleton while fetching

    **Filtering Logic:**
    - All filters combine with AND logic
    - URL params for filter persistence (shareable links)
    - Active filter count badge
    - Preserve filters during pagination

    **What to Avoid:**
    - Don't use external chart libraries (use CSS like other pages)
    - Don't add export functionality yet (Phase 4.1-03)
    - Don't add batch operations (out of scope for this plan)
    - Don't create new API routes — use Prisma Client directly in Server Component
  </action>
  <verify>
    1. Visit http://localhost:3000/dashboard/payments
    2. Verify summary cards show correct totals
    3. Test date range filter — payments filtered correctly
    4. Test amount range filter — only payments in range shown
    5. Test payment method filter — filters work
    6. Test quick chips — "This Month" shows current month payments
    7. Click payment row — navigation works
    8. Test pagination — next/prev buttons work
    9. Test URL params — filters persist in URL
    10. npm run build succeeds without TypeScript errors
  </verify>
  <done>
    - Payments list page renders with summary cards and table
    - All filters work correctly (date, amount, method, customer)
    - Quick filter chips apply correct filters
    - Pagination works with 25 records per page
    - Filters persist in URL params
    - Table is sortable and mobile-responsive
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create payment detail page with invoice and customer context</name>
  <files>app/dashboard/payments/[id]/page.tsx</files>
  <action>
    Create `/dashboard/payments/[id]` dynamic route for payment detail view:

    **Data Fetching:**
    - Query Payment by ID with relations: customer, invoice
    - Handle 404 if payment not found (notFound() from Next.js)
    - Fetch customer's recent payment history (last 5 payments)

    **Layout Structure:**
    - Payment header section:
      - Payment amount (large, prominent)
      - Payment date
      - Transaction reference number (with copy button)
      - Payment method badge
      - Status badge (Applied/Pending/Failed)
      - Source badge (QuickBooks/Manual)
      - QuickBooks ID if synced

    - Invoice Information Card:
      - Invoice number (link to invoice detail)
      - Invoice total amount
      - Invoice balance (before/after this payment)
      - Invoice status
      - Due date

    - Customer Information Card:
      - Customer name (link to customer detail)
      - Email and phone
      - Current QuickBooks balance
      - Total paid to date
      - Payment history count

    - Payment Details Section:
      - Payment date (formatted with time)
      - Payment method
      - Transaction reference
      - Applied to invoice (link)
      - Private notes (if any)
      - Created at / Updated at timestamps
      - Created by user (if tracked)

    - Recent Payment History (same customer):
      - Last 5 payments table
      - Date, Amount, Invoice #, Method
      - Link to view full customer payment history

    - Action Buttons:
      - Back to Payments List
      - View Invoice
      - View Customer
      - Edit Payment (disabled for QB-synced payments)
      - Delete Payment (admin only, disabled for QB-synced)

    **Layout Pattern:**
    - Two-column responsive layout (like invoice detail page)
    - Desktop: Left column (payment details), Right column (invoice + customer cards)
    - Mobile: Single column stacked
    - Consistent spacing and card styling with rest of dashboard

    **What to Avoid:**
    - Don't implement edit/delete functionality yet (Phase 4.1-04)
    - Don't add receipt download (Phase 4.1-03)
    - Don't add refund functionality (Phase 2 - Stripe)
    - Don't query external APIs — use database data only
  </action>
  <verify>
    1. Navigate to specific payment from payments list
    2. Verify all payment details display correctly
    3. Verify invoice card shows correct invoice data with link
    4. Verify customer card shows correct customer data with link
    5. Click "View Invoice" — navigates to invoice detail
    6. Click "View Customer" — navigates to customer detail
    7. Test with QB-synced payment — shows QB ID badge
    8. Test with manual payment — shows Manual badge
    9. Verify recent payment history table renders
    10. Test responsive layout — mobile stacks correctly
    11. npm run build succeeds
  </verify>
  <done>
    - Payment detail page renders with all sections
    - Invoice and customer information cards complete
    - Navigation links work (invoice, customer, back)
    - Recent payment history displays correctly
    - QuickBooks sync status visible
    - Responsive layout works (desktop 2-col, mobile stacked)
    - 404 handled for invalid payment IDs
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add payments navigation link to dashboard sidebar</name>
  <files>components/dashboard/dashboard-sidebar.tsx</files>
  <action>
    Add Payments navigation link to the dashboard sidebar:

    **Navigation Item:**
    - Add between "Invoices" and "Customers" sections
    - Icon: Currency Dollar icon (from Heroicons or Lucide)
    - Label: "Payments"
    - Path: /dashboard/payments
    - Active state when pathname matches /dashboard/payments

    **Icon Styling:**
    - Consistent size with other nav icons
    - Same hover/active states as other items
    - Color: Match dashboard theme (likely text-gray-600 hover:text-blue-600)

    **Mobile Navigation:**
    - Also add to mobile bottom nav if exists
    - Ensure touch target is at least 44x44px

    **What to Avoid:**
    - Don't reorder existing navigation items unless required for logical grouping
    - Don't add badge/counter yet (Phase 4.1-02 may add pending payment count)
    - Don't change existing sidebar structure or behavior
  </action>
  <verify>
    1. View dashboard sidebar — "Payments" link visible
    2. Click "Payments" link — navigates to /dashboard/payments
    3. Verify active state when on payments page
    4. Check icon alignment with other nav items
    5. Test mobile nav — Payments link visible and functional
    6. npm run build succeeds
  </verify>
  <done>
    - Payments link added to sidebar navigation
    - Icon displays correctly and matches dashboard style
    - Active state works when on /dashboard/payments
    - Mobile navigation includes Payments link
    - Navigation is accessible and functional
    - No TypeScript errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds without errors
- [ ] npm run lint passes
- [ ] Visit /dashboard/payments — page loads with data
- [ ] Test all filters — date range, amount range, payment method work
- [ ] Test quick filter chips — apply correct filters
- [ ] Test pagination — navigate between pages
- [ ] Click payment row — navigates to detail page
- [ ] View payment detail — all sections render correctly
- [ ] Click invoice/customer links — navigation works
- [ ] Test sidebar — Payments link navigates correctly
- [ ] Test on mobile viewport — responsive layout works
</verification>

<success_criteria>

- Payments list page complete with filtering and pagination
- Payment detail page complete with invoice and customer context
- Payments navigation link added to sidebar
- All filters persist in URL params (shareable links)
- Responsive layout works on mobile and desktop
- QuickBooks sync status visible throughout
- No TypeScript errors or build warnings
- All navigation links functional
  </success_criteria>

<output>
After completion, create `.planning/phases/4.1-deployment-ready/4.1-01-SUMMARY.md`:

# Phase 4.1 Plan 1: Payments Dashboard Complete

**Finance team can now view and track all payment data in dedicated dashboard page**

## Accomplishments

- Created comprehensive payments list page with filtering, search, and pagination
- Built payment detail page with full transaction context (invoice, customer, history)
- Added payments navigation to dashboard sidebar
- Implemented advanced filtering: date range, amount range, payment method, customer
- Quick filter chips for common workflows (Today, This Week, This Month, High Value)
- QuickBooks sync status indicators throughout
- Responsive layout for mobile and desktop

## Files Created/Modified

- `app/dashboard/payments/page.tsx` - Payments list page with filters and pagination
- `app/dashboard/payments/[id]/page.tsx` - Payment detail page
- `components/dashboard/dashboard-sidebar.tsx` - Added Payments navigation link

## Decisions Made

- Used Server Components with Prisma Client (no API routes needed)
- CSS-based styling (no external chart libraries)
- 25 records per page pagination (consistent with invoices/customers)
- Filter persistence via URL params for shareability
- Two-column layout on desktop, stacked on mobile (consistent pattern)

## Issues Encountered

[Document any issues or None]

## Next Step

Ready for Phase 4.1 Plan 2 — Add loading states, error boundaries, and pagination to Deals/Leads pages
</output>
