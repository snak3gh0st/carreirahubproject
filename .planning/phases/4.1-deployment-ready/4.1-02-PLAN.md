---
phase: 4.1-deployment-ready
type: execute
---

<objective>
Add professional loading states, error boundaries, and pagination to Deals and Leads pages to handle production data volumes and provide polished user experience.

Purpose: Deals and Leads pages currently lack pagination (limited to 50 records) and have no loading/error states. This blocks deployment for teams with large datasets and creates poor UX during data fetching.

Output: Enhanced Deals and Leads pages with pagination, loading skeletons, error boundaries, and empty states.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/dashboard/deals/page.tsx
@app/dashboard/leads/page.tsx
@app/dashboard/invoices/page.tsx
@app/dashboard/customers/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pagination, loading states, and error handling to Deals page</name>
  <files>app/dashboard/deals/page.tsx, app/loading.tsx (if doesn't exist), components/dashboard/deal-table-skeleton.tsx (create)</files>
  <action>
    Enhance `/dashboard/deals` page with production-ready patterns:

    **Pagination:**
    - Add pagination support using searchParams (like invoices/customers pages)
    - Default: 25 deals per page (configurable)
    - Query: `prisma.deal.findMany({ skip, take, orderBy, include: { customer } })`
    - Get total count: `prisma.deal.count()`
    - Calculate total pages: Math.ceil(count / pageSize)
    - Add Previous/Next buttons at bottom
    - Show current page / total pages indicator
    - Disable Previous on page 1, disable Next on last page

    **Loading States:**
    - Create `components/dashboard/deal-table-skeleton.tsx`:
      - Skeleton rows (8 rows) with pulsing gray rectangles
      - Match deal table column structure
      - Use Tailwind animate-pulse
    - Use React Suspense boundary with skeleton as fallback
    - Show loading skeleton during initial data fetch
    - Show inline spinner for pagination changes

    **Error Handling:**
    - Wrap data fetching in try-catch
    - Handle Prisma errors gracefully
    - Display error message card if fetch fails
    - Provide "Retry" button that refreshes page
    - Log errors to console for debugging
    - Don't expose internal error details to users

    **Empty State:**
    - Check if deal count is 0
    - Display friendly empty state message:
      - Icon (empty folder or document icon)
      - Heading: "No deals found"
      - Subtext: "Create your first deal or adjust your filters"
      - Action button: "Create Deal" (can be disabled/coming soon for now)

    **Performance:**
    - Keep existing deal summary metrics at top
    - Maintain current filter functionality
    - Use Server Component — no client-side fetching
    - Consider adding index on Deal.createdAt for pagination performance

    **What to Avoid:**
    - Don't change existing deal creation workflow
    - Don't add advanced filters yet (Phase 4.1-03 if needed)
    - Don't add bulk operations
    - Don't implement optimistic UI updates (keep it simple)
  </action>
  <verify>
    1. Visit /dashboard/deals with >50 deals in database
    2. Verify pagination controls render (Previous/Next)
    3. Click Next — navigates to page 2
    4. Click Previous — returns to page 1
    5. Verify page indicator shows "Page X of Y"
    6. Refresh page — see loading skeleton briefly
    7. Test with 0 deals — empty state renders
    8. Simulate error (bad DB connection) — error message displays
    9. npm run build succeeds
  </verify>
  <done>
    - Deals page has pagination (25 per page)
    - Loading skeleton displays during fetch
    - Error boundary catches and displays errors gracefully
    - Empty state renders when no deals exist
    - Previous/Next buttons work correctly
    - Page indicator shows current/total pages
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pagination, loading states, and error handling to Leads page</name>
  <files>app/dashboard/leads/page.tsx, components/dashboard/lead-table-skeleton.tsx (create)</files>
  <action>
    Enhance `/dashboard/leads` page with same patterns as Deals:

    **Pagination:**
    - Add pagination support via searchParams
    - Default: 25 leads per page
    - Query: `prisma.lead.findMany({ skip, take, orderBy })`
    - Get total count: `prisma.lead.count()`
    - Previous/Next buttons at bottom
    - Current page / total pages indicator

    **Loading States:**
    - Create `components/dashboard/lead-table-skeleton.tsx`:
      - Skeleton rows matching lead table structure
      - 8 skeleton rows with animate-pulse
      - Match lead table columns (name, email, status, score, etc.)
    - Use React Suspense with skeleton fallback
    - Show loading state during initial fetch

    **Error Handling:**
    - Wrap data fetching in try-catch
    - Display error card with retry button
    - Log errors for debugging
    - User-friendly error messages

    **Empty State:**
    - Check if lead count is 0
    - Display empty state:
      - Icon (empty inbox or user icon)
      - Heading: "No leads yet"
      - Subtext: "Leads will appear here when added via chatbot or webhook"
      - Optional: "Learn more" link to docs (if docs exist)

    **Lead-Specific Considerations:**
    - Maintain existing SDR assignment functionality
    - Keep lead qualification score display
    - Preserve status filters if they exist
    - Keep lead source badges

    **What to Avoid:**
    - Don't modify lead creation/assignment logic
    - Don't add advanced filtering yet
    - Don't implement lead qualification UI changes
  </action>
  <verify>
    1. Visit /dashboard/leads with >50 leads
    2. Verify pagination works (Previous/Next)
    3. Test page navigation — correct leads shown per page
    4. Refresh page — loading skeleton appears
    5. Test with 0 leads — empty state displays
    6. Test error scenario — error message shows
    7. Verify page indicator accuracy
    8. npm run build succeeds
  </verify>
  <done>
    - Leads page has pagination (25 per page)
    - Loading skeleton displays during fetch
    - Error handling catches Prisma errors
    - Empty state renders when no leads
    - Pagination controls work correctly
    - Page indicator accurate
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add loading states to existing Invoices and Customers pages</name>
  <files>app/dashboard/invoices/page.tsx, app/dashboard/customers/page.tsx, app/dashboard/payments/page.tsx, components/dashboard/invoice-table-skeleton.tsx (create), components/dashboard/customer-table-skeleton.tsx (create), components/dashboard/payment-table-skeleton.tsx (create)</files>
  <action>
    Add loading skeletons to pages that already have pagination but lack loading states:

    **Invoice Page Loading:**
    - Create `components/dashboard/invoice-table-skeleton.tsx`:
      - Match invoice table structure
      - 10 skeleton rows (invoices have more data per row)
      - Skeleton for summary cards at top
      - Skeleton for status distribution chart
    - Wrap invoice data fetching in Suspense with skeleton fallback

    **Customer Page Loading:**
    - Create `components/dashboard/customer-table-skeleton.tsx`:
      - Match customer table columns
      - 8-10 skeleton rows
      - Skeleton for summary cards
    - Wrap customer data fetching in Suspense

    **Payment Page Loading:**
    - Create `components/dashboard/payment-table-skeleton.tsx`:
      - Match payment table columns (from 4.1-01-PLAN)
      - 8 skeleton rows
      - Skeleton for summary cards at top
    - Wrap payment data fetching in Suspense

    **Skeleton Design Pattern:**
    - Use Tailwind's animate-pulse utility
    - Gray backgrounds: bg-gray-200 dark:bg-gray-700
    - Rounded corners: rounded-md or rounded-lg
    - Height matches actual row height
    - Width variations for visual interest (some cols narrower)

    **Implementation:**
    - Convert existing pages to use Suspense boundaries
    - Keep existing error handling (if any)
    - Maintain all current functionality
    - No changes to data fetching logic — just add loading UI

    **What to Avoid:**
    - Don't change pagination logic (already working)
    - Don't modify filter behavior
    - Don't add new features — this is polish only
    - Don't use external skeleton libraries (pure Tailwind CSS)
  </action>
  <verify>
    1. Visit /dashboard/invoices — loading skeleton appears briefly
    2. Visit /dashboard/customers — loading skeleton appears
    3. Visit /dashboard/payments — loading skeleton appears
    4. Verify skeletons match actual table structures
    5. Test on slow connection (Chrome DevTools throttling) — skeletons visible
    6. Verify smooth transition from skeleton to real data
    7. npm run build succeeds
  </verify>
  <done>
    - Invoice page has loading skeleton
    - Customer page has loading skeleton
    - Payment page has loading skeleton
    - Skeletons match table structures accurately
    - Smooth transition skeleton → data
    - Suspense boundaries working correctly
    - No TypeScript errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] npm run lint passes
- [ ] All dashboard pages have loading states (invoices, customers, payments, deals, leads)
- [ ] Deals page pagination works for >50 records
- [ ] Leads page pagination works for >50 records
- [ ] Test empty states — all render correctly
- [ ] Test error states — error messages display
- [ ] Loading skeletons match table structures
- [ ] No layout shift when loading → data
- [ ] Mobile responsive — pagination controls work on mobile
</verification>

<success_criteria>

- Deals and Leads pages have pagination (25 per page)
- All dashboard pages have loading skeletons
- Error boundaries catch and display errors gracefully
- Empty states render when no data exists
- Pagination controls work correctly (Previous/Next)
- Page indicators accurate (Page X of Y)
- No layout shift during loading
- Mobile-responsive pagination
- No TypeScript errors or build warnings
  </success_criteria>

<output>
After completion, create `.planning/phases/4.1-deployment-ready/4.1-02-SUMMARY.md`:

# Phase 4.1 Plan 2: Loading States & Pagination Complete

**Dashboard now handles production data volumes with professional loading and error states**

## Accomplishments

- Added pagination to Deals page (25 per page, handles unlimited records)
- Added pagination to Leads page (25 per page)
- Created loading skeletons for all 5 dashboard pages (invoices, customers, payments, deals, leads)
- Implemented error boundaries with user-friendly error messages
- Added empty states for all pages (when no data exists)
- Pagination controls with Previous/Next buttons and page indicators
- Suspense boundaries for smooth loading UX

## Files Created/Modified

- `app/dashboard/deals/page.tsx` - Added pagination, loading, error handling
- `app/dashboard/leads/page.tsx` - Added pagination, loading, error handling
- `app/dashboard/invoices/page.tsx` - Added loading skeleton
- `app/dashboard/customers/page.tsx` - Added loading skeleton
- `app/dashboard/payments/page.tsx` - Added loading skeleton
- `components/dashboard/deal-table-skeleton.tsx` - Created
- `components/dashboard/lead-table-skeleton.tsx` - Created
- `components/dashboard/invoice-table-skeleton.tsx` - Created
- `components/dashboard/customer-table-skeleton.tsx` - Created
- `components/dashboard/payment-table-skeleton.tsx` - Created

## Decisions Made

- 25 records per page for all paginated pages (consistent UX)
- Pure CSS skeletons (no external libraries, using Tailwind animate-pulse)
- Server Component data fetching with Suspense boundaries
- Error messages user-friendly (no internal error exposure)
- Empty states encourage action where applicable

## Issues Encountered

[Document any issues or None]

## Next Step

Ready for Phase 4.1 Plan 3 — Mobile responsiveness polish and final deployment checks
</output>
