---
phase: 04.1-user-deployment
plan: 02
type: execute
---

<objective>
Transform dashboard from minimal placeholder to polished, department-specific control center for Finance, Admin, and Commercial users.

Purpose: Make dashboard production-ready so users land on a professional, useful interface tailored to their department's workflow.
Output: Department-specific dashboard views, enhanced main dashboard with insights, Admin monitoring UI for integration health.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-user-deployment/4.1-CONTEXT.md
@.planning/phases/04.1-user-deployment/DISCOVERY.md
@.planning/phases/04.1-user-deployment/4.1-01-SUMMARY.md

# Codebase context
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md

# Current dashboard implementation
@app/dashboard/page.tsx
@app/dashboard/layout.tsx
@lib/auth.ts

**From CONTEXT.md:**
- Finance needs: invoices, payments, accounts, GL data, reconciliation
- Admin needs: data sync status, QB connection health, system monitoring, integration logs
- Commercial needs: leads, deals, pipeline, customer info, revenue tracking

**From Discovery:**
- Current dashboard shows basic counts (leads, deals, invoices)
- No department-specific views (all users see same dashboard)
- Integration log viewer exists as API (/api/integration-logs) but no UI
- QB sync status exists (/dashboard/integrations/sync-status) but minimal
- Existing pages: /dashboard/customers, /dashboard/invoices, /dashboard/leads, /dashboard/deals

**Tech Stack:**
- Next.js 14 App Router with server components
- NextAuth for role-based access (ADMIN, FINANCE, COMMERCIAL roles)
- Tailwind CSS for styling
- Prisma for database queries
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create department-specific dashboard views</name>
  <files>app/dashboard/page.tsx, components/dashboard/finance-dashboard.tsx, components/dashboard/admin-dashboard.tsx, components/dashboard/commercial-dashboard.tsx</files>
  <action>
    Update app/dashboard/page.tsx to detect user role from session (session.user.role).
    Create three department-specific dashboard components in components/dashboard/:

    1. finance-dashboard.tsx:
       - Recent invoices table (last 10) with status, amount, due date
       - Overdue invoices summary card
       - Payment tracking summary (total paid, total outstanding)
       - Quick links: /dashboard/invoices, /dashboard/customers

    2. admin-dashboard.tsx:
       - QB connection status card (connected/disconnected, token expiry)
       - Integration sync status card (last sync time, success/failure)
       - Recent integration logs table (last 20 logs from IntegrationLog)
       - System health indicators (total leads, deals, customers synced)
       - Quick links: /dashboard/integrations, /dashboard/settings/integrations

    3. commercial-dashboard.tsx:
       - Lead pipeline summary (NEW, QUALIFIED, CONVERTED counts)
       - Recent leads table (last 10) with status, score, source
       - Deal pipeline summary (WON, LOST, IN_PROGRESS counts)
       - Conversion rate metric (qualified leads → deals)
       - Quick links: /dashboard/leads, /dashboard/deals

    Render appropriate component based on role. If role not recognized, show generic dashboard.
    Use Tailwind CSS to match existing dashboard styling (white cards, shadows, border-l-4 accents).
  </action>
  <verify>
    - npm run dev starts without errors
    - Visit /dashboard as FINANCE user: see finance-specific view
    - Visit /dashboard as ADMIN user: see admin-specific view
    - Visit /dashboard as COMMERCIAL user: see commercial-specific view
  </verify>
  <done>Department-specific dashboards render correctly based on user role</done>
</task>

<task type="auto">
  <name>Task 2: Add integration log viewer for Admin</name>
  <files>app/dashboard/admin/integration-logs/page.tsx</files>
  <action>
    Create new page at app/dashboard/admin/integration-logs/page.tsx (ADMIN role only).
    Fetch recent integration logs from IntegrationLog table (last 100, ordered by timestamp desc).
    Display in a table with columns: timestamp, service, action, status, duration_ms, error (if failed).
    Add filters for: service (dropdown: all, quickbooks, pipedrive, stripe), status (dropdown: all, success, error).
    Add color coding: green for SUCCESS, red for ERROR, yellow for WARNING.
    Add pagination (20 logs per page) using URL params (?page=1).
    Link from admin-dashboard.tsx "Recent Integration Logs" section.
  </action>
  <verify>
    - Visit /dashboard/admin/integration-logs as ADMIN: see logs table
    - Filter by service: table updates
    - Filter by status: table updates
    - Pagination works (?page=2 shows next 20 logs)
  </verify>
  <done>Integration log viewer accessible to Admin with filtering and pagination</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Department-specific dashboards for Finance, Admin, and Commercial users with enhanced UX</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Login as different roles (create test users if needed via npm run user:create):
       - FINANCE user: Confirm dashboard shows invoices, payments, overdue summary
       - ADMIN user: Confirm dashboard shows QB status, sync health, integration logs link
       - COMMERCIAL user: Confirm dashboard shows leads, deals, pipeline metrics
    3. Test Admin integration log viewer:
       - Visit /dashboard/admin/integration-logs as ADMIN
       - Confirm logs displayed with timestamp, service, action, status
       - Test filters (service, status)
       - Test pagination
    4. Visual check:
       - Dashboard looks polished and professional (not minimal/empty)
       - Styling consistent with existing pages
       - All links work (quick links navigate correctly)
    5. Confirm: Dashboard feels production-ready for user deployment
  </how-to-verify>
  <resume-signal>Type "approved" if dashboard is deployment-ready, or describe UX issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Department-specific dashboard components exist (finance, admin, commercial)
- [ ] app/dashboard/page.tsx routes to correct component based on role
- [ ] Admin integration log viewer page exists and accessible
- [ ] npm run dev succeeds, all pages render without errors
- [ ] User confirms dashboard UX is polished and deployment-ready
</verification>

<success_criteria>

- Department-specific dashboards implemented for FINANCE, ADMIN, COMMERCIAL
- Integration log viewer created for Admin users
- Dashboard UX polished and production-ready
- All quick links functional
- User approves dashboard for deployment
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-user-deployment/4.1-02-SUMMARY.md`:

# Phase 4.1 Plan 2: Dashboard UX Improvements Summary

**[Substantive one-liner - what was built and UX outcome]**

## Accomplishments

- Created department-specific dashboards (Finance, Admin, Commercial)
- Added integration log viewer for Admin monitoring
- Enhanced main dashboard with role-based views
- Polished UI to production-ready state
- [Any additional improvements made]

## Files Created/Modified

### Created
- `components/dashboard/finance-dashboard.tsx` - Finance department view
- `components/dashboard/admin-dashboard.tsx` - Admin department view
- `components/dashboard/commercial-dashboard.tsx` - Commercial department view
- `app/dashboard/admin/integration-logs/page.tsx` - Integration log viewer

### Modified
- `app/dashboard/page.tsx` - Role-based routing to department dashboards

## Decisions Made

- Department-specific views prioritized over generic analytics
- Integration log viewer placed under /dashboard/admin/ (ADMIN-only)
- Consistent Tailwind styling with existing dashboard pages
- Recent items (invoices, leads, logs) limited to 10-20 for performance

## Issues Encountered

[Any issues or "None"]

## Next Step

Phase 4.1 complete - system ready for user deployment with polished dashboard and validated QB data
</output>
