---
phase: 1.1-enhance-invoice-and-customer-dashboard
plan: 02
type: execute
---

<objective>
Enhance invoice detail page to display complete customer information and improve layout for better readability.

Purpose: Finance team needs to see full customer context when viewing an invoice - who the customer is, their contact info, their QuickBooks/Pipedrive IDs, and their overall financial status.
Output: Enhanced invoice detail page with customer info card and improved invoice details layout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior Plan Context
Phase 1.1 Plan 1 created customer detail page with financial summary - this plan enhances the invoice side.

# Codebase Context
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

# Current Implementation
@app/dashboard/invoices/[id]/page.tsx
@prisma/schema.prisma

**Tech Stack Available:**
- Next.js 14+ App Router with React Server Components
- Prisma ORM with PostgreSQL (Neon)
- Tailwind CSS for styling
- TypeScript strict mode

**Established Patterns:**
- Server-side data fetching in page components
- include customer data when fetching invoice: `include: { customer: true }`
- Card-based layout for information display
- Status badges with color coding
- Link components for navigation

**Constraining Decisions:**
- Keep server-side rendering pattern
- Use existing badge and card components
- Match styling from customer detail page (consistency)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check if invoice detail page exists and create/enhance it</name>
  <files>app/dashboard/invoices/[id]/page.tsx</files>
  <action>
  Check if `app/dashboard/invoices/[id]/page.tsx` exists. If it doesn't exist, create it. If it exists, enhance it.

  **Required functionality:**
  1. Fetch invoice with customer data: `prisma.invoice.findUnique({ where: { id }, include: { customer: true, deal: true } })`
  2. Display invoice details in a clean card layout
  3. Show invoice metadata: invoice #, status, amount, due date, paid date (if paid)
  4. Include QuickBooks/Stripe external IDs if present
  5. Auth protection (same as other finance pages)

  **Invoice Details Card Layout:**
  - Invoice Number (or ID if no number)
  - Status badge (with color coding matching invoice list)
  - Amount (formatted as currency)
  - Due Date (with "Overdue" warning if applicable)
  - Payment Date (if paid)
  - QuickBooks ID (if synced)
  - Stripe Payment Intent ID (if paid via Stripe)
  - Created date
  - Updated date

  **What to avoid and WHY:**
  - Don't add edit functionality yet (not in scope) - this plan is read-only view enhancement
  - Don't create client component (consistency) - keep as server component
  - Don't add payment processing UI (future phase) - Phase 2 will handle Stripe integration
  </action>
  <verify>
  1. Navigate to /dashboard/invoices and click any invoice
  2. Invoice detail page loads without errors
  3. All invoice information displays correctly
  4. Status badge shows with correct color
  5. Overdue warning appears for overdue unpaid invoices
  6. Page returns 404 for invalid invoice IDs
  </verify>
  <done>
  - Invoice detail page exists at app/dashboard/invoices/[id]/page.tsx
  - Page displays complete invoice information
  - Status badge color-coded correctly
  - Overdue detection works
  - Auth protection functional
  - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add customer information card to invoice detail page</name>
  <files>app/dashboard/invoices/[id]/page.tsx</files>
  <action>
  Extend the invoice detail page to add a "Customer Information" card section that displays comprehensive customer data.

  **Customer Information Card (add below or beside invoice details):**
  - Section heading: "Customer Information"
  - Customer Name (clickable link to customer detail page `/dashboard/customers/[customerId]`)
  - Email
  - Phone (or "Not provided" if null)
  - Source badges:
    - QuickBooks badge (green) if quickbooks_id present
    - Pipedrive badge (blue) if pipedrive_id present
    - Manual badge (gray) if neither
  - Customer Financial Summary (mini version):
    - Current Balance (qbBalance if available, or calculate from unpaid invoices)
    - Total Invoiced (count of all invoices for this customer)
    - Payment Status indicator (e.g., "3 of 5 invoices paid")

  **Layout:**
  - Use 2-column grid on desktop: Invoice details (left), Customer info (right)
  - Stack vertically on mobile: Invoice details (top), Customer info (bottom)
  - Both cards should have same height and styling for visual balance

  **Link Integration:**
  - Customer name should link to `/dashboard/customers/[customerId]` using Next.js Link component
  - Add "View All Customer Invoices" link at bottom of customer card

  **Implementation notes:**
  - Customer data already fetched via include in Task 1
  - If customer.qbBalance exists, use it; otherwise calculate from invoices
  - To calculate: count invoices where customerId matches, sum unpaid amounts
  - Format phone number if present: display as-is (no formatting logic needed yet)

  **What to avoid and WHY:**
  - Don't add customer edit functionality (not in scope) - read-only view
  - Don't fetch customer invoices separately if not needed (performance) - only fetch if qbBalance is null
  - Don't add customer creation date/metadata (clutter) - focus on financial info
  </action>
  <verify>
  1. Invoice detail page now shows customer information card
  2. Customer name is clickable and links to customer detail
  3. Email and phone display correctly
  4. Source badges show (QuickBooks, Pipedrive, or Manual)
  5. Customer balance displays (from QB or calculated)
  6. Invoice count and payment status are accurate
  7. Layout is responsive (2-col desktop, stacked mobile)
  8. "View All Customer Invoices" link works
  </verify>
  <done>
  - Customer information card added to invoice detail page
  - Customer name links to customer detail page
  - Source badges display correctly
  - Financial summary (balance, invoices) accurate
  - Responsive layout works on mobile and desktop
  - All links functional
  - No TypeScript errors
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Invoice detail page displays complete invoice information
- [ ] Customer information card shows on invoice page
- [ ] Customer name links to customer detail page
- [ ] Status badges and overdue warnings work
- [ ] Financial summary accurate (balance, invoices)
- [ ] Page responsive on mobile and desktop
- [ ] No TypeScript errors in build
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Invoice detail page provides full customer context
- Finance team can quickly navigate between invoice and customer views
- Customer financial status visible from invoice page
- No regressions in existing invoice list page
  </success_criteria>

<output>
After completion, create `.planning/phases/1.1-enhance-invoice-and-customer-dashboard/1.1-02-SUMMARY.md`:

# Phase 1.1 Plan 2: Invoice Page Enhancement with Customer Details

**Enhanced invoice detail page with comprehensive customer information and improved layout**

## Accomplishments

- Created/enhanced invoice detail page with complete invoice information
- Added customer information card with financial summary
- Implemented navigation links between invoice and customer pages
- Responsive two-column layout for desktop, stacked for mobile

## Files Created/Modified

- `app/dashboard/invoices/[id]/page.tsx` - Enhanced invoice detail page

## Decisions Made

[Document any implementation choices made during execution]

## Issues Encountered

[Document any problems and resolutions, or "None"]

## Next Step

Ready for Phase 1.1 Plan 3: Dashboard Graphics & Visual Indicators
</output>
