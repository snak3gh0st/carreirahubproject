---
phase: 1.1-enhance-invoice-and-customer-dashboard
plan: 03
type: execute
---

<objective>
Add visual graphics, charts, and enhanced status indicators to invoice and customer pages for quick financial insights.

Purpose: Finance team needs to see financial trends and status at a glance without reading numbers. Visual indicators (graphs, progress bars, trend indicators) enable faster decision-making.
Output: Invoice and customer pages with visual charts, trend indicators, and enhanced status badges.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior Plan Context
Plans 1-2 created customer and invoice detail pages. This plan adds visual enhancements to existing pages.

# Codebase Context
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

# Current Implementation
@app/dashboard/invoices/page.tsx
@app/dashboard/customers/page.tsx
@app/dashboard/customers/[id]/page.tsx

**Tech Stack Available:**
- Next.js 14+ App Router
- Tailwind CSS for styling
- React Server Components (for data fetching)
- No charting library installed yet

**Established Patterns:**
- Server-side data aggregation
- Card-based layouts
- Tailwind utility classes
- No external UI dependencies (prefer Tailwind + CSS)

**Constraining Decisions:**
- Avoid adding heavy charting libraries (bundle size)
- Use CSS-based visualizations where possible (performance)
- Keep visualizations simple and readable (clarity over complexity)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add simple bar chart visualizations to invoice list page</name>
  <files>app/dashboard/invoices/page.tsx</files>
  <action>
  Enhance the invoice list page summary stats section (5 cards at top) with simple CSS-based progress bars showing proportion of each status.

  **Visual Enhancements:**
  1. **Status Distribution Bar Chart (add above stats cards):**
     - Horizontal stacked bar showing: Draft / Sent / Paid / Overdue proportions
     - Use Tailwind width percentages: `w-[${percentage}%]`
     - Color code segments: Draft=gray, Sent=blue, Paid=green, Overdue=red
     - Show percentage labels on hover (CSS title attribute)
     - Full width bar, segments sized by invoice count proportion

  2. **Amount Progress Bars (add inside each summary card):**
     - Total Value card: Show total with no bar (reference amount)
     - Paid card: Progress bar showing (paidAmount / totalAmount) as green bar
     - Pending card: Progress bar showing (pendingAmount / totalAmount) as yellow bar
     - Overdue card: Progress bar showing (overdueAmount / totalAmount) as red bar
     - Bar height: 4px, rounded, below the amount text

  3. **Trend Indicators (add to summary cards if possible):**
     - Simple up/down indicator based on... (NOTE: We don't have historical data yet)
     - SKIP trend indicators for now - requires time-series data not in schema
     - Add TODO comment: "Add trend indicators when historical snapshots available"

  **Implementation:**
  - Use pure CSS + Tailwind for bars (no JavaScript charting library)
  - Calculate percentages: `(count / totalCount) * 100`
  - Bar structure: `<div class="w-full bg-gray-200 rounded-full h-1"><div class="bg-green-600 h-1 rounded-full" style="width: ${percent}%"></div></div>`
  - Ensure percentages are clamped 0-100

  **What to avoid and WHY:**
  - Don't install Chart.js/Recharts (bundle bloat) - CSS bars are sufficient for this use case
  - Don't add complex animations (distraction) - subtle transitions only
  - Don't add interactive tooltips yet (complexity) - use title attributes for hover text
  </action>
  <verify>
  1. Invoice list page shows status distribution bar chart at top
  2. Each status segment sized correctly (proportional to count)
  3. Summary cards show progress bars under amounts
  4. Progress bars correctly represent proportion of total
  5. Color coding matches status badges (green=paid, yellow=pending, red=overdue)
  6. Bars render correctly on mobile (no overflow)
  </verify>
  <done>
  - Status distribution bar chart added to invoice list
  - Progress bars added to summary cards
  - Colors match established badge colors
  - Percentages calculated correctly
  - Responsive on mobile
  - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add visual payment status indicators to customer detail page</name>
  <files>app/dashboard/customers/[id]/page.tsx</files>
  <action>
  Enhance customer detail page with visual payment status indicators in the financial summary section.

  **Visual Enhancements:**
  1. **Payment Status Pie Chart (CSS-based):**
     - Add a visual circle/ring chart showing paid vs unpaid ratio
     - Use CSS conic-gradient: `background: conic-gradient(green 0% ${paidPercent}%, red ${paidPercent}% 100%)`
     - Size: 120px x 120px circle
     - Place above or beside installment summary
     - Center text: "${paidPercent}% Paid"

  2. **Installment Progress Bar:**
     - Add below "Installment Plan Summary" heading
     - Segmented bar showing: Paid (green) | Remaining (yellow) | Overdue (red)
     - Each segment sized by count proportion
     - Labels: "X paid | Y remaining | Z overdue"

  3. **Enhanced Status Badges in Invoice Table:**
     - Add small colored dot (•) before status text
     - Make overdue invoices have red background (not just text)
     - Add "Days Overdue" count for overdue invoices: "Overdue (15 days)"

  **Implementation:**
  - Calculate paid percentage: `(paidInvoices / totalInvoices) * 100`
  - For days overdue: `Math.floor((Date.now() - new Date(dueDate)) / (1000 * 60 * 60 * 24))`
  - CSS circle: Use div with border-radius: 50%, background: conic-gradient
  - Keep calculations in server component (no client state)

  **What to avoid and WHY:**
  - Don't use SVG charts (complexity) - CSS conic-gradient is simpler and sufficient
  - Don't add client-side interactivity (scope) - static visualizations only
  - Don't calculate complex metrics (premature) - stick to simple counts and percentages
  </action>
  <verify>
  1. Customer detail page shows payment status pie chart
  2. Pie chart correctly represents paid vs unpaid ratio
  3. Installment progress bar shows three segments (paid/remaining/overdue)
  4. Invoice table has enhanced status badges with colored dots
  5. Overdue invoices show "Days Overdue" count
  6. All visualizations render correctly on mobile
  </verify>
  <done>
  - Payment status pie chart added to customer detail
  - Installment progress bar visualizes paid/remaining/overdue
  - Enhanced status badges with colored dots
  - Overdue days calculation working
  - Responsive on mobile
  - No TypeScript errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Visual graphics and indicators added to invoice and customer pages - status distribution bar chart, progress bars, payment status pie chart, and enhanced badges</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/dashboard/invoices
       - Confirm status distribution bar chart appears at top
       - Confirm summary cards show progress bars under amounts
       - Verify colors: green=paid, yellow=pending, red=overdue, gray=draft
       - Check bars are proportionally sized correctly
    3. Visit: http://localhost:3000/dashboard/customers
       - Click any customer with invoices
    4. On customer detail page:
       - Confirm payment status pie chart displays
       - Confirm installment progress bar shows three segments
       - Verify invoice table has enhanced status badges with colored dots
       - Check overdue invoices show "Days Overdue"
    5. Test on mobile (browser responsive mode):
       - Verify all charts/bars display correctly without overflow
       - Confirm text remains readable
    6. Visual check: Do graphics provide quick financial insights? Are colors consistent across pages?
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Status distribution bar chart on invoice list works
- [ ] Progress bars on summary cards accurate
- [ ] Payment status pie chart on customer detail correct
- [ ] Installment progress bar segments sized right
- [ ] Enhanced status badges with dots/days overdue
- [ ] All visualizations responsive on mobile
- [ ] Colors consistent across pages (green=paid, etc.)
- [ ] No TypeScript errors in build
- [ ] User verification passed (checkpoint approved)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Visual graphics provide quick financial insights
- Finance team can see status distribution at a glance
- Payment status clearly visualized on customer pages
- Enhanced indicators make overdue invoices immediately obvious
- No regressions in existing functionality
  </success_criteria>

<output>
After completion, create `.planning/phases/1.1-enhance-invoice-and-customer-dashboard/1.1-03-SUMMARY.md`:

# Phase 1.1 Plan 3: Dashboard Graphics & Visual Indicators

**Added visual charts, progress bars, and enhanced status indicators to invoice and customer pages**

## Accomplishments

- Added status distribution bar chart to invoice list page
- Implemented progress bars in summary cards
- Created payment status pie chart for customer detail page
- Added installment progress bar with segments
- Enhanced status badges with colored dots and days overdue

## Files Modified

- `app/dashboard/invoices/page.tsx` - Added bar chart and progress bars
- `app/dashboard/customers/[id]/page.tsx` - Added pie chart and enhanced indicators

## Decisions Made

[Document any implementation choices made during execution]

## Issues Encountered

[Document any problems and resolutions, or "None"]

## Next Step

Ready for Phase 1.1 Plan 4: Advanced Filtering System
</output>
