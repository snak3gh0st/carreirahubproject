---
phase: 18-client-surveys-intake-and-nps-forms
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/hub/form-templates.ts
  - app/hub/forms/page.tsx
  - app/hub/forms/[id]/page.tsx
autonomous: true
requirements: [SURV-01]

must_haves:
  truths:
    - "`FORM_TEMPLATES` exports `nps-entry` and `nps-exit` without replacing or duplicating the existing onboarding templates"
    - "`NPS_SCORE_FIELD` is the single source of truth for the 0-10 answer key used by all NPS score extraction"
    - "Hub form detail page renders NPS templates through the existing `scale` field path with `scaleMin: 0` and `scaleMax: 10`"
    - "Hub list/detail pages show Portuguese title and description copy when the client language is `pt-BR`"
  artifacts:
    - path: "lib/hub/form-templates.ts"
      provides: "NPS template definitions and shared NPS field/template constants"
      exports: ["FORM_TEMPLATES", "NPS_SCORE_FIELD", "NPS_TEMPLATE_IDS", "getTemplate"]
    - path: "app/hub/forms/page.tsx"
      provides: "Localized hub form list cards for NPS/intake assignments"
    - path: "app/hub/forms/[id]/page.tsx"
      provides: "Localized hub form detail page using the existing scale renderer"
  key_links:
    - from: "app/api/hub/forms/[id]/route.ts"
      to: "lib/hub/form-templates.ts"
      via: "getTemplate(assignment.templateId)"
      pattern: "getTemplate"
    - from: "app/hub/forms/[id]/page.tsx"
      to: "template.fields"
      via: "existing scale renderer"
      pattern: 'field.type === "scale"'
---

<objective>
Add the NPS survey templates and shared score contract to the existing Hub form system, then make sure the Hub list/detail pages display the new forms correctly in Portuguese and English.

Purpose: Phase 18 depends on reusing the current form infrastructure, not inventing a second survey system.
Output: `nps-entry` and `nps-exit` templates are available via `FORM_TEMPLATES`, and the Hub UI displays them correctly through the existing assignment/detail flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-client-surveys-intake-and-nps-forms/18-RESEARCH.md
@.planning/phases/18-client-surveys-intake-and-nps-forms/18-UI-SPEC.md

@lib/hub/form-templates.ts
@app/api/hub/forms/[id]/route.ts
@app/hub/forms/page.tsx
@app/hub/forms/[id]/page.tsx

<interfaces>
From `lib/hub/form-templates.ts`:
```typescript
export interface FormTemplate {
  id: string;
  title: string;
  titlePt: string;
  description: string;
  descriptionPt: string;
  fields: FormField[];
}

export const FORM_TEMPLATES: Record<string, FormTemplate> = { ... };
export function getTemplate(slug: string): FormTemplate | null
```

From `app/hub/forms/[id]/page.tsx`:
```typescript
const isPt = lang === "pt-BR";
const min = field.scaleMin ?? 1;
const max = field.scaleMax ?? 10;
```

From `app/hub/forms/page.tsx`:
```typescript
const tpl = FORM_TEMPLATES[a.templateId];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NPS templates and shared score constants</name>
  <files>lib/hub/form-templates.ts</files>
  <read_first>
    - lib/hub/form-templates.ts
    - .planning/phases/18-client-surveys-intake-and-nps-forms/18-RESEARCH.md
  </read_first>
  <action>
Update `lib/hub/form-templates.ts` without altering any existing template IDs:

1. Export the shared score contract:
```typescript
export const NPS_SCORE_FIELD = "npsScore";
export const NPS_TEMPLATE_IDS = ["nps-entry", "nps-exit"] as const;
```

2. Add two new entries to `FORM_TEMPLATES`:
- `nps-entry`
- `nps-exit`

3. Each NPS template must:
- Use the existing `scale` field type, not a custom widget
- Use `id: NPS_SCORE_FIELD` for the 0-10 answer
- Set `scaleMin: 0` and `scaleMax: 10`
- Include Portuguese and English title/description copy
- Include a short textarea follow-up question (e.g. `npsComment`) for qualitative feedback

4. Keep `getTemplate()` intact and do not add new schema models, migrations, or database-backed template storage.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/carreirahubproject && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - lib/hub/form-templates.ts contains `export const NPS_SCORE_FIELD = "npsScore"`
    - lib/hub/form-templates.ts contains `export const NPS_TEMPLATE_IDS`
    - lib/hub/form-templates.ts contains `"nps-entry": {`
    - lib/hub/form-templates.ts contains `"nps-exit": {`
    - lib/hub/form-templates.ts contains `scaleMin: 0`
    - lib/hub/form-templates.ts contains `scaleMax: 10`
    - lib/hub/form-templates.ts contains `titlePt`
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>NPS templates exist in the shared form registry and the score field contract is exported for downstream ops/NPS extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Localize Hub list/detail rendering for the new survey templates</name>
  <files>app/hub/forms/page.tsx, app/hub/forms/[id]/page.tsx</files>
  <read_first>
    - app/hub/forms/page.tsx
    - app/hub/forms/[id]/page.tsx
    - lib/hub/form-templates.ts
    - .planning/phases/18-client-surveys-intake-and-nps-forms/18-UI-SPEC.md
  </read_first>
  <action>
Update the Hub form pages to display template copy correctly for Phase 18:

1. In `app/hub/forms/page.tsx`, render template titles using the client's language:
```typescript
const title = lang === "pt-BR" ? tpl?.titlePt : tpl?.title;
```

2. In `app/hub/forms/[id]/page.tsx`, render:
- `template.titlePt` / `template.title`
- `template.descriptionPt` / `template.description`

3. Keep the existing `scale` field renderer. Only patch it if necessary so that:
- `scaleMin: 0` / `scaleMax: 10` renders the full 0-10 row
- endpoint labels come from `scaleMinLabelPt` / `scaleMaxLabelPt`
- no custom NPS-only renderer is introduced

4. Do not change any Hub API contracts in this plan; stay inside the existing template/detail rendering path.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/carreirahubproject && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/hub/forms/page.tsx contains `titlePt`
    - app/hub/forms/[id]/page.tsx contains `template.titlePt`
    - app/hub/forms/[id]/page.tsx contains `template.descriptionPt`
    - app/hub/forms/[id]/page.tsx contains `field.scaleMin ?? 1`
    - app/hub/forms/[id]/page.tsx contains `field.scaleMax ?? 10`
    - no custom `nps` field type is introduced
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Hub list/detail pages display the new NPS surveys with correct PT/EN copy while continuing to use the existing scale renderer.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes after the template and Hub rendering changes
- Manual spot-check: assign `nps-entry` to a test client and confirm `/hub/forms` and `/hub/forms/[id]` show the localized title/description and 0-10 scale
</verification>
