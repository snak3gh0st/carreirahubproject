---
phase: 18-client-surveys-intake-and-nps-forms
plan: "03"
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - app/ops/students/[enrollmentId]/StudentProfileClient.tsx
  - app/ops/students/[enrollmentId]/FormsSection.tsx
autonomous: true
requirements: [SURV-03, SURV-04]

must_haves:
  truths:
    - "Student profile shows a Forms section with assigned templates, statuses, and assigned/submitted dates"
    - "Ops user can assign a supported form from the profile and see the profile refresh without a full reload"
    - "Completed NPS entry/exit submissions render as visible score badges on the student profile header"
    - "The Phase 18 flow stays inside the Ops portal; no dashboard-only assign link is required for the primary workflow"
  artifacts:
    - path: "app/ops/students/[enrollmentId]/FormsSection.tsx"
      provides: "Forms list, empty state, and inline assign action for the student profile"
    - path: "app/ops/students/[enrollmentId]/StudentProfileClient.tsx"
      provides: "Student profile header with NPS badges and embedded forms section"
  key_links:
    - from: "app/ops/students/[enrollmentId]/FormsSection.tsx"
      to: "/api/ops/forms/assign"
      via: "React Query mutation"
      pattern: "/api/ops/forms/assign"
    - from: "app/ops/students/[enrollmentId]/FormsSection.tsx"
      to: "['student-profile', enrollmentId]"
      via: "query invalidation after assignment"
      pattern: "student-profile"
    - from: "app/ops/students/[enrollmentId]/StudentProfileClient.tsx"
      to: "npsResults"
      via: "header badge rendering"
      pattern: "npsResults"
---

<objective>
Finish the Ops Hub surface for Phase 18 by adding a Forms section to the student profile and exposing NPS entry/exit results where coordinators already work.

Purpose: The backend wiring is only useful if ops staff can see assignment status and trigger survey assignments without leaving the profile.
Output: `/ops/students/[enrollmentId]` shows forms, supports inline assignment, and surfaces NPS badges in the header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-client-surveys-intake-and-nps-forms/18-RESEARCH.md
@.planning/phases/18-client-surveys-intake-and-nps-forms/18-UI-SPEC.md
@.planning/phases/18-client-surveys-intake-and-nps-forms/18-02-PLAN.md

@app/ops/students/[enrollmentId]/StudentProfileClient.tsx
@app/ops/students/[enrollmentId]/SessionSection.tsx
@app/api/ops/enrollments/[id]/route.ts

<interfaces>
From `app/ops/students/[enrollmentId]/StudentProfileClient.tsx`:
```typescript
function useProfileData(enrollmentId: string) {
  return useQuery<ProfileData>({
    queryKey: ["student-profile", enrollmentId],
    queryFn: () => fetch(`/api/ops/enrollments/${enrollmentId}`).then((r) => r.json()),
  });
}
```

From `app/ops/students/[enrollmentId]/SessionSection.tsx`:
```typescript
const qc = useQueryClient();
qc.invalidateQueries({ queryKey: ["student-profile", enrollmentId] });
toast.success("Sessão registrada com sucesso");
```

From `app/api/ops/enrollments/[id]/route.ts` after Plan 02:
```typescript
return NextResponse.json({
  enrollment,
  placementTest,
  totalSessions,
  workflow,
  availableFormTemplates,
  npsResults,
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the student profile data types and header for NPS visibility</name>
  <files>app/ops/students/[enrollmentId]/StudentProfileClient.tsx</files>
  <read_first>
    - app/ops/students/[enrollmentId]/StudentProfileClient.tsx
    - app/api/ops/enrollments/[id]/route.ts
    - .planning/phases/18-client-surveys-intake-and-nps-forms/18-UI-SPEC.md
  </read_first>
  <action>
Update `StudentProfileClient.tsx` to consume the Phase 18 API additions:

1. Extend `ProfileData` with:
- `enrollment.formAssignments`
- `availableFormTemplates`
- `npsResults`

2. Render completed NPS badges in the header card, beneath the existing CEFR data cluster:
- `NPS Entrada: {score}/10`
- `NPS Saída: {score}/10`

3. Insert a new `<FormsSection />` between the phase timeline and the existing `<SessionSection />`, passing:
- `enrollmentId`
- `customerId`
- `formAssignments`
- `availableFormTemplates`
- `npsResults`

4. Keep the current loading and error states simple; do not introduce a second page-level query.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/carreirahubproject && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - StudentProfileClient.tsx contains `formAssignments`
    - StudentProfileClient.tsx contains `availableFormTemplates`
    - StudentProfileClient.tsx contains `npsResults`
    - StudentProfileClient.tsx contains `NPS Entrada`
    - StudentProfileClient.tsx contains `NPS Saída`
    - StudentProfileClient.tsx renders `FormsSection`
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The student profile header and query types are ready for Phase 18 forms/NPS visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Build FormsSection with inline assign flow</name>
  <files>app/ops/students/[enrollmentId]/FormsSection.tsx</files>
  <read_first>
    - app/ops/students/[enrollmentId]/SessionSection.tsx
    - .planning/phases/18-client-surveys-intake-and-nps-forms/18-UI-SPEC.md
    - app/api/ops/forms/assign/route.ts
  </read_first>
  <action>
Create `app/ops/students/[enrollmentId]/FormsSection.tsx` as a client component that matches the existing student profile style:

1. Use React Query mutation to `POST /api/ops/forms/assign`:
- body `{ customerId, templateId }`
- on success: invalidate `["student-profile", enrollmentId]`
- show `sonner` success/error toasts

2. Render the assigned forms list:
- template name
- status badge (`Pendente`, `Em andamento`, `Concluído`)
- assigned date
- submitted date when present

3. Render an inline assign UI (no modal):
- collapsed by default behind an "Atribuir Formulário" action
- dropdown populated from `availableFormTemplates`
- submit button disabled while pending
- hide or block templates already assigned in a non-completed state

4. Empty state:
- "Nenhum formulário atribuído"
- "Atribua um formulário de entrada ou NPS para este aluno."

5. Keep this component Ops-native. Do not link the primary assign flow back to `/dashboard/forms/*`.
  </action>
  <verify>
    <automated>cd /Users/pauloloureiro/Dev/SigmaProjects/carreirahubproject && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - FormsSection.tsx contains `"use client"`
    - FormsSection.tsx contains `/api/ops/forms/assign`
    - FormsSection.tsx contains `toast.success`
    - FormsSection.tsx contains `invalidateQueries`
    - FormsSection.tsx contains `Atribuir Formulário`
    - FormsSection.tsx contains `Nenhum formulário atribuído`
    - FormsSection.tsx contains `Pendente`
    - FormsSection.tsx contains `Concluído`
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The student profile contains an inline Forms section with assignment and status visibility inside the Ops portal.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes after the profile UI changes
- Manual spot-check: open `/ops/students/[enrollmentId]`, assign `nps-entry`, submit it from `/hub/forms/[id]`, then refresh the profile and confirm the NPS badge appears
</verification>
